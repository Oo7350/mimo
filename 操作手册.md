# Mimo 操作手册

## 产品简介

Mimo 是一款面向小团队的轻量级项目管理工具，支持故事(Story)、任务(Task)、缺陷(Bug)三种工作项类型的差异化管理和团队协作。主要功能包括：团队协作、差异化看板管理（含泳道视图）、用户故事地图、缺陷追踪、项目概览、日报/周报生成、任务评论、站内通知、全局搜索（Ctrl+K）、深色模式。

**访问地址**:
- 本地开发: `http://localhost:5173`
- 线上部署: `http://8.137.189.117`（示例，以实际部署为准）

**界面版本**: v2.13.6 — 邮件发送日志 + Webhook 集成 + 操作审计 + 数据导入导出 (2026-08-21)
**后端版本**: v2.13.6 — 邮件通知收尾（发送日志 + 模板化）+ Webhook + AuditLog + DataPort (2026-08-21)

---

## 一、注册与登录

### 1.1 注册账号

1. 打开登录页面（左侧为产品介绍与看板预览，右侧为表单），点击"立即注册"
2. 填写信息：
   - **用户名**：3-50 个字符，不可重复
   - **邮箱**：需符合邮箱格式
   - **密码**：最少 6 个字符
   - **确认密码**：与密码一致
3. 点击"注册"，成功后跳转到登录页

> 新注册用户默认为"成员"角色，如需管理员权限请联系团队管理员。

### 1.2 登录

1. 在登录页右侧表单输入用户名和密码
2. 点击"登录"
3. 登录成功后进入**工作台**首页

> 登录/注册页采用左右分屏布局：左侧展示产品卖点与模拟看板，右侧为表单区；窄屏下自动隐藏左侧预览。

### 1.3 全局快捷操作

| 功能 | 入口 | 说明 |
|------|------|------|
| **全局搜索** | 顶栏「搜索…」或 **Ctrl+K**（Mac: Cmd+K） | 搜索任务、跳转页面、打开项目、新建工作项 |
| **深色模式** | 顶栏月亮/太阳图标，或个人设置页 | 切换浅色/深色主题，偏好保存在浏览器 |
| **项目切换** | 顶栏下拉（项目相关页面） | 在看板/概览/报告页快速切换项目 |
| **通知** | 顶栏铃铛 | 实时推送；点击任务类通知直达看板并打开详情 |

**Command Palette（全局搜索）** 支持：
- 输入关键词跨项目搜索任务（匹配标题、编号）
- 快捷导航：工作台、项目列表、团队管理、个人设置
- 当前项目：打开看板、报告、新建工作项
- 列出已参与项目，一键进入

### 1.4 退出登录

点击页面右上角头像 → "退出登录"，确认后退出。

**测试账号：**

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin | 123456 | 管理员 |
| zhangsan | 123456 | 成员 |
| lisi | 123456 | 成员 |

---

## 二、工作台（首页）

登录后默认进入工作台，顶部为渐变 **Hero 欢迎区**（按时段问候、逾期/待办提示、快捷按钮），下方为数据面板：

| 区域 | 内容 |
|------|------|
| 统计卡片 | 总任务、进行中、已完成、**完成率（%）** 四张卡片 |
| 活跃 Sprint | 若有进行中的 Sprint，显示名称与「查看看板」入口 |
| 我的项目 | 彩色头像项目列表，点击进入项目概览 |
| 我的待办 | 分配给我的未完成任务，支持悬停快速完成 |
| 近期动态 | 带头像与操作类型标签的时间线 |

待办面板中每个任务显示：类型徽章、标题、编号、所在项目、优先级、截止日期。逾期任务以红色标注。悬停任务右侧出现完成按钮，点击即可标记完成。

Hero 区快捷按钮：**进入项目**、**创建团队**。

---

## 三、个人设置

页面顶部为渐变 **个人信息 Hero**（头像、用户名、邮箱、角色），右侧可切换 **深色/浅色模式**。

### 修改个人信息

1. 点击左侧菜单「个人设置」，或在顶栏头像下拉中选择
2. 在「基本资料」卡片中修改：用户名、邮箱
3. 点击「保存修改」

### 修改密码

1. 在「修改密码」卡片中
2. 输入原密码、新密码（最少 6 位）、确认新密码
3. 点击「修改密码」

---

## 四、团队管理

团队是项目的容器，只有创建或加入团队后才能创建项目。

### 4.1 创建团队

1. 左侧菜单 → "团队管理"
2. 点击"创建团队"
3. 填写：
   - **团队名称**（必填）
   - **描述**（可选）
4. 点击"确定"
5. 创建者自动成为团队管理员

### 4.2 查看团队

进入「团队管理」，以 **卡片网格** 展示所有团队：顶部为 **翠绿渐变 Hero 区域**（带装饰光球背景），下方是横向列表式团队卡片（左侧绿色渐变色条 + 头像环 + 名称/描述 + 成员数 + 右侧箭头）。点击卡片进入团队详情。

> 团队页面采用**绿色主题**（#10b981 / #047857），与项目页面的靛蓝主题形成视觉区分。

### 4.2.1 删除团队 ⚠️ 仅所有者

每个团队卡片右侧有 **「删除」** 按钮（红色文字按钮，hover 时显示）。点击后弹出确认对话框：

- **操作者限制**：仅团队**创建者（所有者）** 可删除
- **级联效果**：删除团队成员关系；关联的**项目不会被删除**（项目独立存在）
- **不可恢复**：确认后团队及成员关系永久删除

### 4.3 团队详情

团队详情页采用 **左右双栏布局**：

- 顶部 **Hero 横幅**（翠绿渐变遮罩）：团队名、描述、成员数、**群聊按钮**（带未读消息角标）、邀请成员按钮
- **左栏**：
  - **团队成员** 列表（头像 + 用户名/邮箱 + 角色标签 + 移除操作）
  - **关联项目** 卡片网格（Key 标签 + 项目名 + **删除按钮**，点击进入看板）
- **右栏（固定群聊面板）**：实时聊天，详见 §4.6

### 4.4 邀请成员 ⚠️ 需管理员

1. 进入团队详情页
2. 点击"邀请成员"
3. 输入要邀请的用户 ID，选择角色（管理员 / 成员）
4. 点击"确定"

> 页面底部会提示已注册用户的 ID 供参考。

### 4.5 移除成员 ⚠️ 需管理员

1. 团队详情页的成员列表中
2. 点击目标成员行的"移除"按钮
3. 确认后移除

> 管理员不可被移除。

### 4.6 团队群聊（v2.2 新增）

团队详情页右侧为 **固定群聊面板**，支持团队成员实时文字聊天。

**面板结构：**
- 顶部：群聊图标 + "团队群聊" 标题 + 在线人数
- 中间：消息列表（自动滚动到底部）
- 底部：输入框 + 发送按钮

**消息气泡：**
| 发送者 | 样式 |
|--------|------|
| 自己 | 绿色渐变背景、白色文字、**右对齐**、显示时间 |
| 其他成员 | 灰色背景、深色文字、**左对齐**、显示发送者名称 + 时间 |

**功能特性：**
- 进入团队页面时自动加载最近 100 条历史消息
- 通过 WebSocket 实时接收新消息，无需刷新
- 不同日期的消息之间显示 **日期分隔线**
- Hero 区域的「群聊」按钮显示 **未读消息数量角标**（红色），点击可跳转到聊天区域
- 滚动到聊天底部时自动清除未读计数
- **Enter 键发送**，支持多行输入（Shift+Enter 换行）
- 窄屏（<900px）时聊天面板自动下移至左栏下方
- **Emoji 选择器（v2.4 新增）**：输入框左侧 😊 按钮，点击弹出 4 类 Emoji 面板（表情/手势/物品/符号，200+ 个），点击即插入到光标位置
- **消息撤回（v2.5 新增）**：鼠标悬停自己发送的消息，右上角出现「撤回」按钮；**仅 2 分钟内**可撤回；已撤回消息显示灰色斜体"消息已撤回"；撤回后通过 WebSocket 同步给所有在线成员
- **@提及成员（v2.5 新增）**：在输入框中输入 `@` 字符，自动弹出团队成员选择面板（支持模糊搜索）；点击成员名后在光标位置插入 `@username`；消息中 `@xxx` 渲染为绿色高亮

### 4.7 单设备登录（v2.5 新增）

同一账号同时只允许一个设备在线。当账号在 **新设备** 登录时：

- 新设备正常获取 Token 并登录成功
- **旧设备**的下次请求会收到 **401 错误**："账号已在其他设备登录"
- 前端自动清除本地用户信息并跳转到登录页

> 此功能基于 Redis 实现：每次登录将新 Token 写入 Redis (`mimo:token:{userId}`)，请求时校验请求头 Token 与 Redis 中存储的是否一致。

---

## 五、项目管理

### 5.1 创建项目 ⚠️ 权限说明

**管理员操作（直接创建）：**
1. 进入某个团队详情页
2. 在右侧项目区域点击"创建项目"
3. 填写：项目名称、项目 Key、模板（Scrum 或 Kanban）
4. 点击"确定"

**普通成员操作（需走审批）：**
1. 进入团队详情页，点击"创建项目"
2. 填写项目信息后提交
3. 系统自动创建一条 **PROJECT_CREATE 类型的审批请求**
4. 等待管理员在「审批管理」页面审核通过后，项目才会被实际创建

> 后端强制校验：非管理员直接调用 `POST /api/projects` 会返回 **403 Forbidden**。前端会自动引导走审批流程。

**自动降级容错（v2.7.2 新增）**：
- 即使前端判断为管理员，如果后端因权限变更等原因返回 **403 或 500**，前端会**自动降级为审批请求**
- 非管理员提交审批请求失败时，显示友好提示"提交审批请求失败，请稍后重试"，而非通用的"服务器错误"
- 三层防御确保成员操作项目时不会出现 500 错误

创建后自动生成 3 个看板列：**待办** / **进行中** / **已完成**。

### 5.2 查看我的项目

左侧菜单 → 「项目管理」→「项目列表」，或直接在工作台「我的项目」区域点击进入。

**重要变更（v2.7）**：项目列表现在显示用户所在团队内的**所有项目**，而不仅仅是自己创建或被加入的项目。团队成员创建的任何项目都会在列表中可见，方便团队协作。

项目列表以 **卡片网格** 展示，每张卡片含项目头像、模板标签（Scrum/Kanban）、Key、团队名，以及看板 / Sprint / 报告快捷入口。

### 5.2.1 删除项目 ⚠️ 权限说明

每个项目卡片底部有 **「删除」** 按钮（红色文字按钮）。点击后弹出确认对话框：

- **管理员**：确认后直接删除
- **普通成员**：后端返回 **403 Forbidden**，前端引导走审批流程（创建 PROJECT_DELETE 类型审批请求）

**级联删除**：同时清理该项目的所有关联数据（看板列、任务/缺陷、项目成员关系、报告、Sprint）。不可恢复。

### 5.2.2 审批结果通知反馈（v2.7.2 新增）

当管理员审批通过或拒绝成员的审批请求后，系统会通过**双通道**通知申请人：

| 通知通道 | 触发时机 | 效果 |
|---------|---------|------|
| **右上角铃铛** | 页面加载时自动获取未读数 | 红点角标 + 下拉列表显示所有通知（含审批结果） |
| **弹窗提示** | 登录进入系统后（MainLayout 加载完成） | 右上角 ElNotification 弹窗，绿色=通过、橙色=拒绝，多条错开 500ms 显示 |

**通知内容示例：**
- 通过：「您的审批请求「创建项目: Mimo开发」已被管理员通过」
- 拒绝：「您的审批请求「删除项目: 测试项目」被管理员拒绝，原因：项目正在使用中」

> 审批通知由后端 `ApprovalService` 在 approve/reject 时自动写入 `notifications` 表，前端 `MainLayout.vue` 的 `checkApprovalNotifications()` 在页面挂载时拉取并展示。

---

## 六、项目概览

进入项目后首先展示概览页面。顶部为 **项目 Hero**（彩色头像、项目名称、新建工作项按钮）。

概览 Tab 内包含：

| 区块 | 内容 |
|------|------|
| 统计摘要 | 总任务、进行中、已完成、完成率 四个统计 pill |
| 任务状态分布 | 环形饼图（待办/进行中/已完成） |
| 成员工作量 | 横向柱状图 + 成员列表（头像、任务数、完成率进度条） |
| 即将到期 | 近 3 天截止的未完成任务（含类型图标，逾期红色） |
| 燃尽图 | 活跃 Sprint 名称 + 理想 vs 实际剩余故事点趋势线 |
| 最近活动 | 带头像的时间线列表 |

概览页顶部 Tab：**概览** / **看板** / **报告**。

看板页面内置三种视图（分段控制器切换）：

| 视图 | 说明 |
|------|------|
| 看板 | 经典 Kanban 三列，支持泳道分组与筛选预设 |
| 缺陷列表 | BUG 专属表格视图，按严重程度/状态筛选排序 |
| 故事地图 | STORY 按史诗(epic)分组横向排列，点击展开子任务 |

> 燃尽图显示 Sprint 的理想进度线（虚线）与实际进度线（实线含填充色），直观反映迭代是否偏离计划。

### 6.1 Sprint 管理页（v2.9.1 新增）

访问路径：项目列表 → 进入项目 → 顶部 Tab **概览** 旁的「Sprint 管理」入口 / 或直接访问 `/projects/{id}/sprints`。

**页面布局**：左侧 Sprint 列表 + 右侧燃尽图详情。

**Sprint 列表卡片**包含：
- 状态徽章（规划中 / 进行中 / 已完成）
- 起止日期
- 名称与目标
- 任务/完成数/完成率
- 操作按钮（启动 / 完成 / 手动快照）

**创建 Sprint**：点击右上「+ 新建 Sprint」按钮，填写名称、目标、起止时间即可。
- 若项目已有 ACTIVE 状态的 Sprint，新建后状态为 PLANNING
- 点击「启动」激活 Sprint（自动停用同项目其他活跃 Sprint 并生成首日快照）
- 点击「完成」结束 Sprint（未完成任务自动回退到 Backlog）
- 点击「快照」手动生成今日燃尽快照

**燃尽图**：理想线（虚线灰色）+ 实际线（实线品牌紫含渐变填充），并智能判断"领先 / 正常 / 滞后"。

**团队工时分布**（燃尽图下方）：按成员聚合当前 Sprint 的累计工时，横向柱状条按总工时归一化展示，一目了然谁投入多、谁少。

### 6.2 工时记录（v2.9.2 新增）

访问路径：打开任意 Issue → 切换到「**工时**」Tab。

**工时列表**：
- 头像 + 用户名 + 工时（小时）
- 日期 + 描述
- 仅创建人/团队管理员可看到删除按钮

**累计工时**（顶部右侧）：当前 Issue 的所有工时之和。

**添加方式 1 — 手动添加工时**：
- 点击「+ 添加工时」按钮
- 填写日期、小时（可小数）、说明
- 保存

**添加方式 2 — 计时器**（推荐）：
- 点击「开始」按钮，计时器开始走表（带脉动动画）
- 工作中可「暂停」/「继续」
- 完成时点击「停止并记录」→ 自动打开创建对话框预填小时数和说明
- 不想保存可「取消」

**数据权限**：
- 团队成员可查看 / 创建
- 仅创建人 / 团队管理员可删除
- 单次工时上限 24 小时

**关联能力**：
- Sprint 管理页 → 选中 Sprint → 燃尽图下方展示该 Sprint 的「团队工时分布」
- 报表模块后续版本会扩展更多工时相关图表

---

## 七、看板与工作项管理

看板是日常工作的核心界面，支持三种工作项类型（故事/任务/缺陷）在同一个看板中混合显示，每种类型有差异化的卡片样式和专属功能。

### 7.1 三种工作项类型概览

| 类型 | 卡片边框色 | 图标 | 适用场景 |
|------|----------|------|----------|
| 故事 (STORY) | 绿色 | 📖 | 从用户视角描述需求，可拆分为多个子任务 |
| 任务 (TASK) | 蓝色 | ✅ | 具体开发工作，可挂载到某个故事下 |
| 缺陷 (BUG) | 红色 | 🐛 | 功能异常或错误报告，有独立状态流 |

### 7.2 看板视图切换

看板页面顶部工具栏包含：返回概览、项目名称、**报告按钮**（带近 7 天报告数量角标）、**新建工作项下拉菜单**（含「生成今日日报」「生成本周周报」快捷入口）、**分段控制器**（看板 / 缺陷列表 / 故事地图）。

> 点击「报告」按钮或角标可跳转到项目的报告与数据看板页面。

**看板Tab切换（v2.6 新增）**

看板区域顶部新增 **Tab 切换栏**，将工作项和缺陷分开显示：

| Tab | 图标 | 内容 | 说明 |
|-----|------|------|------|
| 工作项 | 📊 | 故事(STORY) + 任务(TASK) | 经典三列布局（待办/进行中/已完成） |
| 缺陷 | ⚠️ | 缺陷(BUG) 独立状态列 | 6列独占全宽（新建/已确认/修复中/已解决/已验证/已关闭） |

- Tab 栏右侧显示各类型任务数量统计
- 切换Tab时自动联动类型筛选按钮
- 缺陷Tab下每列显示对应BUG状态的任务

**看板底部 — 项目概览统计栏（v2.2 新增）**

看板列下方显示一行 **项目概览数据**，实时统计当前看板的任务分布：

| 统计项 | 颜色 | 说明 |
|--------|------|------|
| 总任务 | 深色 | 所有任务总数 |
| 待办 | 灰色 | TODO 状态任务数 |
| 进行中 | 橙色 | IN_PROGRESS 状态任务数 |
| 已完成 | 绿色 | DONE 状态任务数 |
| 缺陷 | 红色 | BUG 类型任务数 |

右侧有 **「查看报告与数据看板 →」** 链接，点击跳转到报告页面。点击任意数字也可跳转。

**看板视图**（默认）
经典 Kanban 三列布局：待办（TODO）/ 进行中（IN_PROGRESS）/ 已完成（DONE）。列头显示彩色顶条与任务计数。三种类型的卡片在同一列中混排，通过不同的左边框颜色、图标和摘要信息区分。空列显示「暂无任务」提示。

**缺陷列表视图**
以表格形式展示所有 BUG，列包含：编号、标题、严重程度、状态、优先级、指派人、发现版本、创建时间。支持按严重程度和状态筛选，点击行打开详情。

**故事地图视图**
以史诗(Epic)为单位分组展示所有 STORY。每个史诗下的故事横向排列为一行。点击故事卡片可展开其关联的子任务列表。适合从宏观视角规划需求。

### 7.2.1 泳道（Swimlanes）

在看板筛选栏中选择 **泳道** 模式，可在每一列内按维度分组显示卡片：

| 模式 | 说明 |
|------|------|
| 无泳道 | 默认平铺，按拖拽顺序排列 |
| 按指派人 | 列内按成员分组，「未指派」单独一组 |
| 按史诗 | Story 按 Epic 分组，非 Story 归入「其他类型」 |

每组显示分组标题（头像/图标 + 名称 + 任务数）。开启泳道后列内自动按分组排序；关闭泳道后恢复原始顺序。

### 7.2.2 卡片快捷操作

鼠标悬停任务卡片时，右上角出现快捷按钮：
- **✓ 标记完成**：非已完成状态可用，一键设为 DONE
- **✏ 编辑**：打开任务详情弹窗

### 7.2.3 筛选预设

筛选栏提供一键预设按钮，可快速切换常用筛选：

| 预设 | 效果 |
|------|------|
| 全部 | 清除所有筛选 |
| 缺陷 | 仅显示 BUG |
| 故事 | 仅显示 STORY |
| 我的 | 仅显示指派给当前用户的任务 |

可与搜索框、指派人、优先级、类型按钮组合使用；手动修改筛选后预设高亮自动取消。

### 7.3 创建故事 (STORY)

1. 点击"新建工作项"，选择"故事"类型
2. 填写故事三要素：
   - **作为（用户角色）**：谁需要这个功能，如"管理员"
   - **我希望（目标功能）**：需要什么能力，如"导出月度报表"
   - **以便（业务价值）**：能带来什么价值，如"分析团队效率趋势"
   - 系统会自动将三个要素拼接为故事标题
3. **史诗 (Epic)**：输入归属的史诗/特性分组，如"报表模块"。同史诗的故事会在故事地图中归为一组
4. **故事点**：从 1/2/3/5/8/13/21 中选择工作量估算
5. **验收标准 (AC)**：
   - 在"验收标准"区域输入文字后回车或点击 + 添加
   - 每条 AC 可勾选完成/未完成
   - 创建模式下本地编辑，编辑模式下实时保存到后端
6. 其他字段：指派人、优先级、截止日期、描述、标签与 TASK 一致
7. 点击"创建"——故事卡片会显示验收标准进度条和子任务计数

### 7.4 创建任务 (TASK)

1. 点击"新建工作项"，选择"任务"类型
2. 填写标题、优先级、指派人、故事点、截止日期等
3. **所属故事**：如果有已创建的故事，可从下拉中选择将任务挂载到该故事下
4. 点击"创建"——任务卡片显示指派人、截止日期（逾期红色高亮）、父故事编号

### 7.5 创建缺陷 (BUG)

1. 点击"新建工作项"，选择"缺陷"类型
2. 填写缺陷报告要素：

| 字段 | 说明 |
|------|------|
| 严重程度 | 阻塞/严重/主要/次要/轻微 |
| 环境信息 | 操作系统、浏览器、App 版本（分三个输入框，自动拼接） |
| 期望结果 | 正确的行为应该是什么 |
| 实际结果 | 实际发生了什么错误 |
| 复现步骤 | 详细的步骤编号说明 |
| 发现版本 | 在哪个版本发现的 |
| 修复版本 | 在哪个版本修复（可后续填写） |

3. 缺陷创建后自动设为"新建(NEW)"状态
4. BUG 卡片显示严重程度徽章、当前状态、环境摘要

### 7.6 缺陷状态流转

BUG 拥有独立的 7 级状态流，不跟 STORY/TASK 共用 TODO-DONE：

```
NEW（新建）→ CONFIRMED（已确认）→ IN_PROGRESS（修复中）
→ RESOLVED（已解决）→ VERIFIED（已验证）→ CLOSED（已关闭）
```

- 任一状态都可以 REOPENED（重新打开）回到 IN_PROGRESS
- NEW 也可直接 CLOSED（不是缺陷）
- 状态变更会自动校验转换规则，不合法的操作会被拒绝
- 编辑 BUG 时状态下拉框只显示当前状态下允许的选项

### 7.7 拖拽任务

直接将任务卡片从一个列拖到另一个列，松开即可完成状态变更。拖拽时卡片半透明跟随鼠标，原位置显示虚线占位符，目标列高亮虚线边框。

**状态验证（v2.6 修复）**：
- STORY/TASK：自由拖动到任意列
- BUG 类型：系统会校验缺陷状态转换规则（见 §7.6），不合法的转换只更新位置不更新状态

**多端实时同步**：同一看板内，任一用户拖拽任务后，其他在线用户的视图自动更新，无需手动刷新。系统通过 WebSocket 广播任务移动事件。

### 7.7.1 删除任务（v2.6 修复）

任务卡片悬停时右上角显示 **删除按钮**（🗑），点击后弹出确认对话框：

- **级联清理**：同时删除该任务的评论、附件、标签、相关通知
- **子任务处理**：如有子任务（parentId指向该任务），子任务的父子关系会解除（parentId设为null）
- **实时同步**：删除后通过 WebSocket 广播，其他在线用户视图自动更新
- 支持在任务详情弹窗内和看板卡片上两种方式删除

### 7.8 编辑任务

点击任务卡片 → 弹出详情弹窗 → 修改信息 → 点击"保存"。弹窗内容根据工作项类型动态切换：
- **STORY**：可修改三要素、史诗、故事点、验收标准
- **TASK**：可修改指派人、截止日期、父故事
- **BUG**：可修改严重程度、环境、期望/实际结果、缺陷状态、修复版本

### 7.9 任务筛选

看板筛选工具栏（卡片式横条）提供以下筛选项，可组合使用：

| 筛选项 | 说明 |
|--------|------|
| 搜索框 | 按标题/编号/指派人/标签模糊搜索 |
| 指派人 | 下拉选择具体成员 |
| 优先级 | 最高/高/中/低/最低 |
| 泳道 | 无泳道 / 按指派人 / 按史诗 |
| 类型按钮 | 全部 / 故事 / 任务 / 缺陷（彩色切换） |
| 筛选预设 | 全部 / 缺陷 / 故事 / 我的 |

### 7.10 任务评论

在任务编辑弹窗的"评论"Tab 中：
- 查看该任务的所有讨论
- 输入评论内容，Ctrl+Enter 快捷发送
- 评论支持 Markdown 格式（加粗、列表、链接等）
- 新评论即时追加，无需刷新
- **删除评论** ⚠️ v2.7.3 新增：仅评论作者可见「删除」按钮，点击后弹出确认对话框

---

## 八、附件管理

### 8.1 上传附件

1. 打开任务详情弹窗（编辑模式）
2. 切换到"详细描述与附件"Tab
3. 点击"上传附件"，选择文件

> 单文件最大 10MB。

### 8.2 下载 / 删除附件

在附件列表中点击下载或删除按钮。

---

## 九、通知系统

顶部栏铃铛图标显示未读通知数量，通过 WebSocket 实时推送，无需等待即可收到新通知。

触发通知的场景：
- 任务被分配给你
- 你负责的任务被他人修改了状态
- 你负责的任务收到新评论

点击铃铛展开通知面板：
- 未读通知左侧有蓝色圆点标识
- **点击任务类通知**：自动跳转到该任务所在项目的看板页，并打开任务详情弹窗；同时标记已读
- 顶部「全部已读」按钮一键清理

> 也可通过 **Ctrl+K** 全局搜索任务编号或标题，快速定位工作项。

---

## 十三、团队管理员与权限（v2.7 新增）

### 13.1 权限体系

Mimo 采用**两级权限体系**：

| 角色 | 范围 | 说明 |
|------|------|------|
| **系统管理员 (ROLE_ADMIN)** | 全局 | 拥有所有团队的完全控制权，可设置用户等级 |
| **团队管理员 (ROLE_ADMIN)** | 单个团队 | 可邀请/移除成员、审批请求 |
| **普通成员 (ROLE_MEMBER)** | 团队内 | 基本操作，部分操作需审批 |

> 系统admin自动拥有所有团队的管理员权限。

### 13.2 审批流程（v2.7 新增，v2.7.1 强化）

非管理员执行敏感操作时，需要向团队管理员提交审批请求。**后端已强制校验权限，非管理员无法绕过前端直接调用接口。**

**触发审批的操作：**
- 创建项目（`POST /api/projects` → 403 → 走审批）
- 删除项目（`DELETE /api/projects/{id}` → 403 → 走审批）
- 修改项目信息
- 添加项目成员

**完整审批流程：**

```
普通成员发起操作
    ↓
前端检测 isTeamAdmin = false
    ↓
自动创建审批请求 POST /api/approvals (targetType=PROJECT_CREATE/DELETE)
    ↓
管理员在「审批管理」页面查看待审批列表
    ↓
┌─ 通过 PUT /api/approvals/{id}/approve → 后端自动执行原操作
├─ 拒绝 PUT /api/approvals/{id}/reject → 状态标记 REJECTED
└─ 申请人可撤回 PUT /api/approvals/{id}/withdraw（仅 PENDING 状态）
```

**审批管理入口：**
- **系统 admin**：左侧菜单「审批管理」→ 显示 **全系统所有待审批请求**（跨团队）
- **团队管理员**：团队详情页内查看本团队的待审批列表

### 13.3 用户等级系统（v2.7 新增）

系统支持 **L1-L4** 四个等级，由系统管理员统一管理。

| 等级 | 名称 | 颜色 | 图标 | 说明 |
|------|------|------|------|------|
| L1 | 初级成员 | #909399 灰色 | 无 | 默认等级，基础样式 |
| L2 | 正式成员 | #67C23A 绿色 | ⭐ | 微光效果 |
| L3 | 资深成员 | #409EFF 蓝色 | 🏅 | 光晕+渐变边框 |
| L4 | 核心成员 | #E6A23C 金红渐变 | 👑 | 旋转光效+闪烁 |

**等级铭牌展示位置：**
- 个人设置页面的用户名旁（个人信息 Hero 区域）
- 等级越高，铭牌UI越华丽（L4 有动态旋转光效）

### 13.4 等级管理页面（v2.7 新增，v2.7.2 修复）

**访问路径**：`/admin/levels`（仅系统管理员可见）

页面功能：
- **显示所有注册用户**（v2.7.2 修复：不再遗漏无等级记录的用户，未设置等级的默认显示 L1）
- 等级说明图例（L1-L4）
- 下拉选择器快速修改任意用户的等级
- 修改后立即生效并刷新列表

---

## 十四、API 接口索引（v2.7 更新）

报告页含 **报告列表** 与 **数据看板** 两个 Tab，顶部为统一 PageHeader 标题区。

### 10.1 生成报告

1. 在看板页面点击「报告」，或从项目概览 Tab 进入
2. 在「报告列表」Tab 点击「生成报告」
3. 选择类型（日报/周报）和日期
4. 系统自动统计已完成任务生成报告

报告列表以 **表格卡片** 展示，支持按类型筛选。

### 10.2 编辑与提交

- 生成的报告为"草稿"状态，可点击"查看" → "保存"修改内容
- 确认无误后点击"提交"，变为"已提交"状态
- 已提交的报告不可再编辑

### 10.3 导出报告

- 在报告列表中点击"导出"按钮，可直接导出 PDF
- 打开报告详情后点击"导出 PDF"按钮，将 Markdown 内容转为 PDF 文件下载
- 文件名格式：`report-{日期}.pdf`

### 10.4 数据看板

报告页面切换"数据看板"Tab，可查看三项统计数据：

| 图表 | 说明 |
|------|------|
| 本周完成任务趋势 | 近 7 天每日完成数折线图 |
| 成员任务完成分布 | 各成员完成数量饼图 |
| Sprint 任务完成率 | 最近 Sprint 的故事点柱状图 |

---

## 十一、角色与权限速查

| 操作 | 管理员 | 成员 |
|------|--------|------|
| 创建团队 | 可 | 可 |
| 邀请/移除成员 | 可 | 不可 |
| **创建项目** | **直接创建** | **需审批（后端403拦截）** |
| **删除项目** | **直接删除** | **需审批（后端403拦截）** |
| 创建/编辑任务 | 可 | 可 |
| 删除任务 | 可 | 可 |
| 添加评论 | 可 | 可 |
| 生成报告 | 可 | 可 |
| 上传附件 | 可 | 可 |
| 修改个人设置 | 可 | 可 |

---

## 十二、工作项类型详解

### 故事 (STORY)

用户故事是从用户视角描述的功能需求，可拆分为多个子任务。

**专属字段：**

| 字段 | 说明 | 示例 |
|------|------|------|
| 用户角色 | "作为"谁 | 管理员 |
| 目标功能 | "我希望"做什么 | 导出月度报表 |
| 业务价值 | "以便"达成什么 | 分析团队效率趋势 |
| 史诗 (Epic) | 所属功能分组 | 报表模块 |
| 故事点 | 工作量估算 | 1/2/3/5/8/13/21 |
| 验收标准 (AC) | 可勾选的通过条件列表 | "支持PDF和Excel格式" |

---

## 附录：工程结构（v2.12.0 — Maven 多模块拆分）

> v2.12.0 起，后端由单模块工程拆分为 Maven 多模块工程，按"基础类库 / RBAC 子系统 / 业务启动器"三层组织。该改造不引入任何用户可见的新功能，只影响代码组织与构建链。

**模块拓扑**：

```
mimo-backend (parent pom, packaging=pom)
  ├── mimo-common   ← 基础类库（异常 / Result / 注解 / 分页 DTO），零业务依赖
  ├── mimo-system   ← RBAC 子系统（角色 / 权限 / 用户角色分配 / @RequirePermission 切面），依赖 mimo-common
  └── mimo-app      ← 启动器 + 业务实现（Project / Issue / Board / Sprint / Chat / Calendar / Notification / Approval / Dashboard / Report 等），依赖 mimo-common + mimo-system
```

**各模块定位**：

| 模块 | 内容 | 说明 |
|------|------|------|
| `mimo-common` | `BusinessException` / `Result` / `ResultCode` / `RequirePermission` / `IdRequest` / `PageRequest` | 仅 lombok + validation 依赖，保持轻量 |
| `mimo-system` | `Permission` / `Role` / `RolePermission` / `UserRole` 4 实体 + 4 Mapper + `RoleService` + `RoleController` + `RoleDTO` + `PermissionAspect` | RBAC 子系统，可独立编译复用 |
| `mimo-app` | `MimoApplication`（启动类） + 其余所有业务实体 / Mapper / Service / Controller / Config / DTO | 启动器，依赖上述两个模块 |

**对使用者的影响**：

- **最终用户**：零影响。前端 API 路径、请求/响应格式、数据库 schema 全部保持不变。
- **运维部署**：构建命令仍是 `cd backend && mvn clean install`，输出可执行 jar 位置不变。
- **开发者**：
  - 在 IDEA 打开 `backend/pom.xml` 可识别多模块结构，按模块组织 Project 视图
  - 修改 RBAC 相关代码需进入 `mimo-system/`，修改基础类需进入 `mimo-common/`，其余仍在 `mimo-app/`
  - 暂未抽取的 `mimo-project` 子模块（Project / Issue / Board / Sprint 等）仍位于 `mimo-app`，下一阶段继续

**构建验证**：`cd backend && mvn clean install -q` exit 0，三模块全部编译 + 安装本地仓库成功。

**下一步计划**：阶段 4 — 抽取 `mimo-project` 子模块（涉及 Project / Issue / Board / Sprint / Workflow / Comment / Attachment / WorkLog / Approval / Dashboard / Report 等业务域，含跨模块依赖梳理）。

**看板卡片信息：** 📖 图标、故事标签、史诗标签；摘要显示用户角色和故事点；进度条显示验收标准完成率（如 3/5）；底部显示子任务计数。

**故事地图视图：** 按 Epic 分组横向排列，点击展开子任务。适合从宏观视角做需求规划。

### 任务 (TASK)

具体的开发工作项，可挂载到某个故事下作为子任务。

**看板卡片信息：** ✅ 图标、任务标签、父故事编号（如有）；摘要显示指派人头像和截止日期（逾期红色高亮）。

**与故事的关系：** 创建时可选择"所属故事"，将任务挂载到 STORY 下。

### 缺陷 (BUG)

功能异常或错误的完整报告，拥有独立的状态生命周期。

**专属字段：**

| 字段 | 说明 | 示例 |
|------|------|------|
| 严重程度 | 影响等级 | BLOCKER(阻塞)/CRITICAL(严重)/MAJOR(主要)/MINOR(次要)/TRIVIAL(轻微) |
| 环境信息 | 复现环境 | Windows 10; Chrome 120; v1.2.0 |
| 期望结果 | 正确行为 | 正常跳转到首页 |
| 实际结果 | 错误行为 | 页面白屏，控制台报错 |
| 复现步骤 | 步骤编号 | 1. 打开登录页 2. 输入账号密码 3. 点击登录 |
| 发现版本 | 缺陷来源 | 1.2.0 |
| 修复版本 | 修复发布 | 1.2.1 |

**缺陷状态流：**

```
NEW → CONFIRMED → IN_PROGRESS → RESOLVED → VERIFIED → CLOSED
  ↘ CLOSED                       ↘ REOPENED ↗  (重新打开)
```

**看板卡片信息：** 🐛 缺陷标签、严重程度徽章（红/橙/蓝/灰）、状态标签、环境摘要、发现版本。

**缺陷列表视图：** 表格展示所有 BUG，按严重程度/状态筛选排序，点击行打开详情。

### 优先级

| 优先级 | 含义 | 颜色 |
|--------|------|------|
| Highest | 需立即处理 | 红色 |
| High | 高优先级 | 橙色 |
| Medium | 默认 | 默认 |
| Low | 低优先级 | 蓝色 |
| Lowest | 最低优先级 | 蓝色 |

> 可使用 AI 分析功能自动推荐优先级。

---

## 十三、典型工作流程

**Scrum 模式：**
```
1. 创建团队 → 邀请成员
2. 创建项目
3. 在故事地图中按 Epic 规划用户故事
4. 为每个故事添加验收标准
5. 将故事拆分为具体任务，挂载到故事下
6. 启动 Sprint，将故事/任务加入迭代
7. 在看板中拖拽任务流转状态
8. 在概览页通过燃尽图跟踪进度
9. 生成日报/周报总结
```

**缺陷管理模式：**
```
1. 发现缺陷 → 在看板或缺陷列表中创建 BUG
2. 填写完整缺陷报告（环境、期望/实际结果、复现步骤）
3. 确认缺陷 → 状态从 NEW 转为 CONFIRMED
4. 分配开发人员 → 状态从 CONFIRMED 转为 IN_PROGRESS
5. 修复完成 → 状态转为 RESOLVED
6. 测试验证 → 状态转为 VERIFIED（或 REOPENED 重新修复）
7. 关闭缺陷 → 状态转为 CLOSED
```

---

## 十五、AI 助手（v2.8.0 新增）

Mimo 集成了 **DeepSeek AI Copilot**，提供智能辅助功能，帮助团队提升工作效率。

### 15.1 全局 AI 悬浮窗

- **入口**: 页面右下角 **"Mimo AI"** 悬浮按钮（所有页面可见）
- **功能**: SSE 流式对话，支持 Markdown 渲染输出
- **使用**: 点击展开对话框，输入问题即可获得 AI 回复
- **模型**: 默认使用 DeepSeek，支持切换模型
- **快捷键**: 无（悬浮窗常驻）

### 15.2 看板页 — AI 智能创建任务

- **入口**: 看板页顶部 **「AI 创建」** 按钮
- **功能**: 自然语言 → 结构化任务
- **示例输入**: "用户登录后页面白屏，Chrome复现，紧急"
- **AI 自动填充**:
  - 任务类型: BUG
  - 优先级: HIGHEST
  - 严重程度: BLOCKER
  - 描述: 含环境信息模板
  - 建议指派人

### 15.3 任务详情页 — AI 拆分子任务

- **入口**: Story 类型任务的详情弹窗中 **「AI 拆分」** 按钮
- **功能**: 根据 Story 的用户故事和验收标准，AI 建议子任务列表
- **输出**: 子任务标题、类型(TASK)、优先级列表
- **操作**: 可逐个确认或批量添加到当前项目

### 15.4 其他 AI 功能

| 功能 | 入口 | 说明 |
|------|------|------|
| 润色描述 | 任务创建/编辑时 | 将口语化描述转为专业文案 |
| 推荐优先级 | 任务创建/编辑时 | 根据标题+描述分析 urgency/importance |
| 故事点估算 | Story 详情 | 根据验收标准复杂度估算 SP |
| 使用量查询 | AI 悬浮窗 | 显示今日/总计调用次数和 Token 用量 |

> **注意**: AI 功能需要后端配置 `DEEPSEEK_API_KEY` 环境变量。未配置时会提示"AI 服务未配置"。

> **v2.9.0 修复**: 修复了 AI 助手发送长消息或连续发送消息时浏览器控制台出现 `net::ERR_ABORTED` 噪音的问题。后端 SSE 改用原生 `AsyncContext`，前端在发送新消息前自动终止上一次未完成的请求。

---

## 十七、日历管理（v2.8.0 上线，v2.9.0 增强）

> **v2.8.0**: 月/周/日视图 + 事件 CRUD + 任务截止日期自动同步
> **v2.9.0**: 团队可见性 + 项目关联 + 团队/事件类型筛选

### 17.1 进入日历

- 点击侧边栏 **日历** 菜单
- 默认显示月视图，可切换为周视图 / 日视图
- 通过顶部 `<` `>` 按钮切换月份/周/日

### 17.2 四种事件类型

| 类型 | 说明 | 来源 |
|------|------|------|
| `TASK_DEADLINE` | 任务截止日期 | 任务 `dueDate` 变更时自动同步 |
| `SPRINT` | Sprint 起止 | Sprint 创建/编辑时自动生成 |
| `MEETING` | 会议 | 用户手动创建 |
| `REMINDER` | 提醒 | 用户手动创建 |
| `CUSTOM` | 自定义 | 用户手动创建 |

### 17.3 创建事件

1. 在日历上点击空白日期 → 弹出 **新建事件** 对话框
   - 或点击日期格右上角 `+` 按钮
2. 填写：
   - **标题** (必填)
   - **团队**：从下拉列表选择，可选（设为团队可见）
   - **项目**：从下拉列表选择，会根据所选团队级联显示该团队下的项目
   - **开始时间 / 结束时间** (必填)
   - **全天事件**：勾选后隐藏时分
   - **类型**：MEETING / REMINDER / CUSTOM（手动创建时可指定）
   - **地点**、**颜色**、**参与人员**、**提前提醒**
3. 点击 **保存** 即可
4. 在 **项目概览** 页面也可以点 **创建事件** 按钮，会自动把当前项目绑定到事件

### 17.4 编辑 / 删除事件

- 点击日历上的事件块 → 弹出详情
- 仅 **创建者本人** 可以编辑或删除
- 任务截止日期类型 (`TASK_DEADLINE`) 跟随任务 `dueDate` 自动更新，不要手动编辑

### 17.5 可见性规则（v2.9.0 新增）

| 事件配置 | 谁能看见 |
|----------|----------|
| 仅自己创建（无 teamId/projectId） | 只有自己 |
| 标记了 `teamId` | 创建者 + 该团队所有成员 |
| 标记了 `projectId` | 创建者 + 该项目所有成员 |
| 同时标记了 teamId + projectId | 创建者 + 该团队/项目任意一个的成员 |

### 17.6 筛选（v2.9.0 新增）

日历顶部新增两个筛选器：
- **团队筛选**：选择后只显示该团队相关事件
- **事件类型筛选**：选择后只显示 `MEETING` / `REMINDER` / `CUSTOM` / `TASK_DEADLINE` 等指定类型

可叠加使用，叠加时为 **与** 关系。

### 17.7 与任务的联动

- 创建/编辑任务时设置 `dueDate` → 自动在日历上生成 `TASK_DEADLINE` 事件
- 修改任务 `dueDate` → 自动更新对应事件的 `startTime` / `endTime`
- 删除任务 → 自动删除对应的 `TASK_DEADLINE` 事件
- 任务 `dueDate` 置空 → 自动清除对应事件

---

## 十六、常见问题

**Q: 什么是项目 Key？**
A: 项目 Key 是英文大写简称（如 `MIMO`），用于生成任务编号 `MIMO-1`、`MIMO-2`。创建后不可修改。

**Q: 看板列能自定义吗？**
A: 目前固定为待办/进行中/已完成三列，保证流程简洁清晰。

**Q: 评论支持什么格式？**
A: 支持 Markdown 基本语法：加粗、斜体、列表、链接、代码块等。

**Q: 通知多久刷新一次？**
A: 通知通过 WebSocket 实时推送，无需等待。登录后自动连接，收到新通知即时显示。

**Q: 看板拖拽后为什么其他人的视图不变？**
A: 如果多端实时同步没有生效，请检查 WebSocket 连接是否正常。正常情况下同一看板内拖拽后所有用户视图自动更新。

**Q: 如何导出报告？**
A: 在报告列表或报告详情中点击"导出 PDF"按钮，浏览器自动下载 PDF 文件。

**Q: AI 润色和优先级推荐怎么用？**
A: 创建或编辑任务时，在"详细描述"旁点击"✨ 润色"可优化描述文案；在"基本信息"中点击"🤖 分析"可获取 AI 推荐的优先级。AI 建议仅供参考，用户决定是否采纳。

**Q: AI 功能需要什么配置？**
A: v2.7.3 起 AI 功能改为后端代理模式（`/api/ai/polish`、`/api/ai/priority`），API Key 存储在服务端 `application.yml`，前端无需配置。由 DeepSeek 的大模型提供润色和优先级分析能力。

**Q: 缺陷和任务有什么区别？**
A: 缺陷(BUG)有独立的 7 级状态流（新建→确认→修复中→已解决→已验证→已关闭→重新打开）、完整缺陷报告字段（环境/期望结果/实际结果/复现步骤/发现版本/修复版本），卡片样式和筛选视图也不同。任务(TASK)使用简单的 TODO→IN_PROGRESS→DONE 三状态，可挂载到某个故事下。

**Q: 故事地图怎么用？**
A: 在看板页面点击顶部"故事地图"按钮切换。故事按 Epic（创建时填写的史诗字段）分组横向排列，点击故事卡片展开其关联的子任务列表。适合做需求规划和梳理。

**Q: 如何使用全局搜索？**
A: 点击顶栏「搜索…」或按 Ctrl+K（Mac 为 Cmd+K），输入关键词可跨项目搜索任务；也可选择页面导航或快捷操作。↑↓ 选择，Enter 确认，Esc 关闭。

**Q: 看板泳道是什么？**
A: 在筛选栏「泳道」下拉中选择「按指派人」或「按史诗」，每一列内的卡片会按该维度分组显示，便于查看成员负载或 Epic 进度。拖拽改状态的行为与无泳道模式相同。

**Q: 如何切换深色模式？**
A: 点击顶栏月亮/太阳图标，或在「个人设置」页 Hero 区切换。偏好保存在浏览器 localStorage（键名 `mimo-theme`）。

**Q: 缺陷列表视图和看板视图的关系？**
A: 两个视图共享同一数据源。缺陷列表是 BUG 的表格视图，方便按严重程度/状态排序筛选。在看板视图创建的 BUG 也会出现在缺陷列表中，反之亦然。


---\n\n> 手册版本：v2.9.1  Sprint 燃尽图前端可视化 (SprintView 管理页 + BurndownChart 组件 + ProjectOverview 当前活跃 Sprint 卡片) (2026-06-18)


---\n\n> 手册版本：v2.9.2  工时记录系统 (work_logs 表 + Issue 工时 Tab + 计时器 + Sprint 团队工时分布) (2026-06-18)


---\n\n## 十二、甘特图（v2.9.3 新增）

访问路径：项目概览页  顶部「甘特图」按钮。

### 12.1 设置计划时间

打开任意 Issue  底部「**计划时间（甘特图）**」分组：
- **计划起**：计划开始日期
- **计划止**：计划结束日期
- **依赖 Issue ID**：逗号分隔的前置任务 ID，如 1,5,8`n
只有同时设置了起止日期的 Issue 才会出现在甘特图中。

### 12.2 甘特图视图

- **纵轴**：项目下所有设置了计划起止的任务（按开始时间排序）
- **横轴**：时间线
- **条形颜色**：按状态上色（灰=待办、黄=进行中、绿=完成）
- **进度条**：背景浅紫表示总时长，前景深色表示当前进度（基于今日日期）
- **依赖关系**：用浅灰曲线连接前置任务  当前任务
- **类型筛选**：右上角可按「全部/故事/任务/缺陷」过滤

### 12.3 使用场景

- 排期冲突检测：一眼看到重叠任务
- 依赖链路追踪：找到关键路径上的瓶颈
- 进度汇报：截屏给老板看进展

---

## 十二、甘特图（v2.9.3 新增）

访问路径：项目概览页 → 顶部「甘特图」按钮。

### 12.1 设置计划时间

打开任意 Issue → 底部「计划时间（甘特图）」分组：
- 计划起：计划开始日期
- 计划止：计划结束日期
- 依赖 Issue ID：逗号分隔的前置任务 ID，如 `1,5,8`

只有同时设置了起止日期的 Issue 才会出现在甘特图中。

### 12.2 甘特图视图

- 纵轴：项目下所有设置了计划起止的任务（按开始时间排序）
- 横轴：时间线
- 条形颜色：按状态上色（灰=待办、黄=进行中、绿=完成）
- 进度条：背景浅紫表示总时长，前景深色表示当前进度（基于今日日期）
- 依赖关系：用浅灰曲线连接前置任务 → 当前任务
- 类型筛选：右上角可按「全部/故事/任务/缺陷」过滤

### 12.3 使用场景

- 排期冲突检测：一眼看到重叠任务
- 依赖链路追踪：找到关键路径上的瓶颈
- 进度汇报：截屏给老板看进展

## 附录：邮箱排障指南（v2.13.1 新增，v2.13.2 扩充）

针对 IMAP 绑定与收件箱拉取的常见错误，提供诊断工具和排障路径。

### E.1 绑定前先「测试连接」

绑定新邮箱弹窗底部有 3 个按钮：取消 / **测试连接** / 绑定。
- 测试连接只验证凭据，不保存到数据库
- 测试成功后再点「绑定」，避免存入无效账户

### E.2 已绑定账户的「诊断」按钮

已绑邮箱列表「操作」列新增「诊断」按钮（黄色文字）。
点击后弹出：
```
account=xxx@qq.com username=xxx@qq.com password=16 chars, 前2位=ab 末2位=xy
```
- `username` 必须是完整邮箱地址
- `password` 长度应为 16 位（QQ 授权码标准）
- 前 2 位/末 2 位用于对比绑定时输入的授权码

### E.3 QQ 邮箱 -120 错误排障路径

错误码 -120 是 QQ 邮箱返回的通用登录失败码，包含 5 种可能：

| 序号 | 原因 | 解决方案 |
|------|------|---------|
| 1 | IMAP 用户名不是完整邮箱 | 必须填 `xxx@qq.com`，不能只填 QQ 号 |
| 2 | 密码用了 QQ 登录密码 | 必须用 **16 位授权码** |
| 3 | 授权码已失效（改密/长时间未用） | 重新生成新的 16 位授权码 |
| 4 | 频率限制（短时间多次登录） | 停止刷新，等 10-15 分钟自动解除 |
| 5 | 账号异常/风控 | 登录 mail.qq.com 做一次安全验证 |

> 注：v2.13.2 之前出现的 `-120`/`-150`/`-154` 等错误码实际上是 Java `NegativeArraySizeException` 的消息字符串（与 limit 取值相关），并非 QQ 邮箱协议错误码。v2.13.2 已修复该 API 误用 Bug。如果再次遇到这种数字错误码，先升级到 v2.13.2+，再排查 QQ 邮箱本身。

### E.4 各邮箱授权码获取入口

- **QQ 邮箱**：mail.qq.com → 设置 → 账户 → IMAP/SMTP 服务 → 生成授权码（需发短信验证）
- **163 邮箱**：mail.163.com → 设置 → POP3/SMTP/IMAP → 开启 IMAP → 设置客户端授权密码
- **Gmail**：myaccount.google.com → 安全 → 两步验证 → 应用专用密码
- **Outlook/Hotmail**：直接用账号密码即可，无需授权码

### E.5 收件箱拉取超时（v2.13.2 新增）

现象：弹出"拉取收件箱失败：timeout of 15000ms exceeded"。

| 可能原因 | 排查/解决 |
|---------|----------|
| 单次拉取邮件过多 | 减小 limit，前端默认 20 |
| 邮箱服务器响应慢 | 已在 listInbox 用 `FetchProfile` 批量预取元数据，性能提升约 15 倍 |
| 客户端代码逐封拉取正文（v2.13.2 之前） | v2.13.2 起 listInbox 不再拉取 snippet，只返回元数据；如需正文预览点开单封详情 |
| 前端超时阈值过低 | v2.13.2 已将 `getInbox` 单独设置为 30 秒超时 |

### E.6 预设自动填充下拉无数据（v2.13.2 新增）

现象：绑定新邮箱弹窗的"选择预设自动填充"下拉为空。

排查路径：
1. 后端是否启动 → F12 Network 看 `/api/email/presets` 是否 200
2. 接口 200 但下拉空 → 刷新页面（v2.13.2 已改为打开弹窗时自动补拉一次）
3. 接口 403 → localStorage 里的 token 已失效，重新登录即可

### E.7 邮件通知配置（v2.13.3 新增）

v2.13.3 上线"事件触发邮件通知"通道。在邮箱账户列表点「通知偏好」按钮，可配置以下项：

| 配置项 | 说明 |
|--------|------|
| 启用通知 | 总开关，关闭后该账户不再发任何邮件通知 |
| 触发事件 | 5 类可选：任务指派、@提及、审批结果、任务评论、任务状态变更 |
| SMTP 主机 | 留空时按 IMAP 主机自动推断（imap.x → smtp.x） |
| SMTP 端口 | 留空时默认 465（SSL） |
| 发送测试邮件 | 不依赖偏好开关，直接用该账户 SMTP 给本人发一封测试邮件 |

**典型配置**：
- QQ 邮箱：smtp.qq.com:465（SSL），授权码同 IMAP
- 163 邮箱：smtp.163.com:465（SSL），客户端授权密码同 IMAP
- Gmail：smtp.gmail.com:465（SSL），应用专用密码同 IMAP

**收件人来源**：邮件发到收件人用户的 `User.email` 字段（在个人资料中设置）。若该字段为空，测试邮件会回退发到绑定的邮箱地址本身。

**触发链路**：站内通知（任务指派/审批/@提及/评论/状态变更）→ NotificationService.create → mapEmailType 映射 → EmailNotificationService.dispatch（@Async）→ 用户偏好过滤 → MailSenderService.send。

**失败降级**：
- 邮件发送失败不影响站内通知落库
- 单个账户发送失败不影响其他账户
- 派发异常被 NotificationService 包了 try-catch Throwable，绝对不会影响主流程

**常见问题**：

| 现象 | 排查 |
|------|------|
| 测试邮件返回 500 | 检查授权码是否同时支持 SMTP（QQ 默认支持，需在网页版设置中确认"SMTP 服务"已开启） |
| 测试返回成功但收不到 | 检查垃圾邮件箱；QQ 邮箱自发邮件可能被拦截，需加白名单 |
| 没收到通知邮件但站内通知有 | 检查偏好开关是否启用、对应事件类型是否勾选 |

### E.8 浏览器侧 403 但后端直连可用（v2.13.2 新增）

现象：PowerShell 直连同 token 调 `/api/email/*` 返回 200，但浏览器侧返回 403。

原因：后端 JWT_SECRET 已更换（如重启时换了环境变量），但浏览器 localStorage 里还是旧 token，签名不匹配被 filter 拦截。

解决：清掉 localStorage 的 `token` 字段后重新登录（F12 → Application → Local Storage → 删除 token → 刷新）。

### E.9 邮件发送日志查询（v2.13.6 新增）

入口：左侧菜单「系统管理」分组下「邮件发送日志」（仅 `ROLE_ADMIN` 可见）。

用途：
- 排查"用户说没收到邮件"问题：按 userId 或 toEmail 筛选，看 success=0 行的 error 字段。
- 监控送达率：顶部统计卡片显示总数 / 成功 / 失败 / 送达率（百分比）。
- 字段含义：`notifyType` 标识事件类型（assignment / mention / approval / comment / issue_status），`relatedId` 关联通知 ID，可回到通知列表查原始内容。

### E.10 Webhook 集成（v2.13.5 新增）

入口：左侧菜单「系统管理」分组下「Webhook 集成」（仅 `ROLE_ADMIN` 可见）。

用途：将任务变更事件推送到钉钉/飞书/Jenkins 等外部系统。

操作流程：
1. 点「新建 Webhook」按钮，填写名称 / URL / 签名密钥 / 订阅事件 / 生效范围。
2. 保存后点「测试投递」按钮，弹窗展示响应状态码、响应体、耗时、错误信息。
3. 点「投递日志」按钮查看该 Webhook 的历史投递记录，包含完整 payload 和响应体。

订阅事件类型（逗号分隔）：
- `ISSUE_CREATED`：任务创建
- `ISSUE_UPDATED`：任务更新
- `ISSUE_DELETED`：任务删除

对方服务端校验签名：读取 `X-Mimo-Signature` 请求头，用配置的 secret 字符串作为密钥，对请求体做 HmacSHA256 哈希，hex 输出后比对。一致则可信。

### E.11 操作审计日志（v2.13.4 新增）

入口：左侧菜单「系统管理」分组下「操作审计日志」（仅 `ROLE_ADMIN` 可见）。

用途：记录所有敏感操作（如创建/删除项目、修改权限、Webhook 配置变更等），含 IP / User-Agent / RequestId 串联同一请求的多条操作。

筛选条件：操作人 / 对象类型 / 动作 / 对象 ID / RequestId / 时间范围。

导出：点「导出 CSV」按钮下载当前筛选结果。

### E.12 数据导入导出（v2.13.4 新增）

入口：项目概览页顶部「导入」「导出」按钮。

**导出**：选择格式（Excel / CSV / JSON），后端直接触发文件下载。可用于跨环境数据迁移、报表归档。

**导入**：上传 Excel/CSV 文件，自动校验字段类型和必填项，错误行跳过并返回错误列表。建议先下载模板按格式填数据再上传。

---

> 手册版本：v2.13.6  邮件发送日志 + Webhook 集成 + 操作审计 + 数据导入导出 (2026-08-21)
