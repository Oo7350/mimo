# Mimo API 接口文档

## 基础信息

- **Base URL**: `http://localhost:8080`
- **认证方式**: JWT Bearer Token（请求头 `Authorization: Bearer <token>`）
- **响应格式**: 统一包装 `Result<T>`

```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

- **Swagger 文档**: `http://localhost:8080/swagger-ui.html`
- **全局请求格式**: 除附件上传接口外，所有 POST/PUT 请求均需 `Content-Type: application/json`，请求体为 JSON 格式

---

## 认证说明

### 公开接口（无需 Token）
- `POST /api/auth/login`
- `POST /api/auth/register`
- Swagger 相关路径

### 需要认证的接口
其余所有接口需在请求头携带：
```
Authorization: Bearer <token>
```

Token 有效期 24 小时，过期需重新登录。

### 角色权限

| 角色 | 说明 |
|------|------|
| `ROLE_ADMIN` | 系统管理员 / 团队管理员 |
| `ROLE_MEMBER` | 普通成员 |

**需管理员权限的操作：**
- 邀请成员加入团队
- 移除团队成员
- **直接创建项目**（非管理员需走审批流程，后端返回 403）
- **直接删除项目**（非管理员需走审批流程，后端返回 403）

---

## 全局错误码

| 错误码 | 含义 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未认证 / Token 过期或无效 / **账号已在其他设备登录** (v2.5+) |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 409 | 冲突 |
| 500 | 服务器内部错误 |
| 1001 | 用户名已存在 |
| 1002 | 邮箱已存在 |
| 1003 | 用户不存在 |
| 1004 | 密码错误 |
| 1005 | Token 过期 |
| 1006 | Token 无效 |
| 1101 | 团队不存在 |
| 1102 | 团队名已存在 |
| 1103 | 已在团队中 |
| 1104 | 无权操作团队 |
| 1201 | 项目不存在 |
| 1202 | 项目名已存在 |
| 1301 | 列下仍有任务 |
| 1302 | 列不存在 |
| 1401 | Sprint 不存在 |
| 1402 | 已有活跃 Sprint |

---

## 一、认证模块

### 1.1 用户注册

```
POST /api/auth/register
Content-Type: application/json
```

> 注意：必须使用 POST 方法，且 Content-Type 必须为 application/json，否则会返回 404 或 500 错误。

**请求体：**
```json
{
  "username": "string (3-50字符，必填)",
  "password": "string (最少6字符，必填)",
  "confirmPassword": "string (必填，需与 password 一致)",
  "email": "string (邮箱格式，必填)"
}
```

**响应（成功）：**
```json
{
  "code": 200,
  "message": "注册成功"
}
```

**响应（参数校验失败）：**
```json
{
  "code": 400,
  "message": "具体错误信息，如：用户名已存在 / 两次密码不一致 / 参数格式错误"
}
```

---

### 1.2 用户登录

```
POST /api/auth/login
Content-Type: application/json
```

**请求体：**
```json
{
  "username": "string",
  "password": "string"
}
```

**响应：**
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbG...",
    "userInfo": {
      "id": 1,
      "username": "admin",
      "email": "admin@mimo.dev",
      "avatar": null,
      "role": "ROLE_ADMIN"
    }
  }
}
```

---

## 二、用户模块

### 2.1 获取个人信息

```
GET /api/user/profile
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "username": "admin",
    "email": "admin@mimo.dev",
    "avatar": null,
    "role": "ROLE_ADMIN",
    "createdAt": "2024-01-01 00:00:00"
  }
}
```

### 2.2 修改个人信息

```
PUT /api/user/profile
Content-Type: application/json
```

**请求体：**
```json
{
  "username": "string (3-50字符)",
  "email": "string",
  "avatar": "string (可选)"
}
```

### 2.3 修改密码

```
PUT /api/user/password
Content-Type: application/json
```

**请求体：**
```json
{
  "oldPassword": "string",
  "newPassword": "string (最少6字符)",
  "confirmPassword": "string"
}
```

---

## 三、仪表盘模块

### 3.1 获取仪表盘数据

```
GET /api/dashboard
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "username": "admin",
    "totalTasks": 10,
    "inProgressTasks": 3,
    "completedTasks": 5,
    "myProjects": [
      {
        "id": 1,
        "name": "Mimo开发",
        "key": "MIMO",
        "teamName": "Mimo开发团队"
      }
    ],
    "activeSprints": [
      {
        "id": 1,
        "name": "Sprint 1",
        "projectName": "Mimo开发",
        "totalIssues": 8,
        "completedIssues": 4
      }
    ],
    "recentActivities": [
      {
        "id": 1,
        "username": "admin",
        "action": "CREATE",
        "detail": "创建了任务 MIMO-1: 用户登录功能",
        "createdAt": "2024-01-15 10:30:00"
      }
    ]
  }
}
```

---

## 四、团队模块

### 4.1 创建团队

```
POST /api/teams
Content-Type: application/json
```

**请求体：**
```json
{
  "name": "string (必填)",
  "description": "string (可选)"
}
```

创建者自动成为团队管理员。

### 4.2 我的团队列表

```
GET /api/teams/my
```

**响应：**
```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "name": "Mimo开发团队",
      "description": "核心开发团队",
      "ownerId": 1,
      "ownerName": "admin",
      "memberCount": 3,
      "createdAt": "2024-01-01 00:00:00"
    }
  ]
}
```

### 4.3 团队详情

```
GET /api/teams/{id}
```

### 4.4 团队成员列表

```
GET /api/teams/{id}/members
```

**响应：**
```json
{
  "code": 200,
  "data": [
    {
      "userId": 1,
      "username": "admin",
      "email": "admin@mimo.dev",
      "avatar": null,
      "role": "ROLE_ADMIN",
      "joinedAt": "2024-01-01 00:00:00"
    }
  ]
}
```

### 4.5 邀请成员 ⚠️ 需管理员

```
POST /api/teams/invite
Content-Type: application/json
```

**请求体：**
```json
{
  "teamId": 1,
  "userId": 2,
  "role": "ROLE_MEMBER"
}
```

### 4.6 移除成员 ⚠️ 需管理员

```
DELETE /api/teams/{teamId}/members/{userId}
```

### 4.7 删除团队 ⚠️ 仅团队所有者

```
DELETE /api/teams/{teamId}
Authorization: Bearer <token>
```

**响应：**
```json
{
  "code": 200,
  "message": "团队已删除"
}
```

> 级联删除团队成员关系。关联项目不会被删除，项目独立存在。

---

## 五、项目模块

### 5.1 创建项目 ⚠️ 需管理员或走审批

```
POST /api/projects
Content-Type: application/json
```

**请求体：**
```json
{
  "name": "string (必填)",
  "key": "string (必填，大写，唯一，如 MIMO)",
  "description": "string (可选)",
  "template": "SCRUM | KANBAN",
  "teamId": 1
}
```

**权限说明：**
- **团队管理员 / 系统管理员（ROLE_ADMIN）**：直接创建成功
- **普通成员**：后端返回 `403 Forbidden`，提示"只有管理员可以创建项目，请通过审批申请"。前端应引导用户走审批流程（`POST /api/approvals`，targetType=`PROJECT_CREATE`）

创建后自动生成 3 个默认看板列：待办、进行中、已完成。同时自动将团队成员加入项目成员表。

### 5.2 我的项目列表

```
GET /api/projects/my
```

### 5.3 项目详情

```
GET /api/projects/{id}
```

### 5.4 团队下的项目

```
GET /api/projects/team/{teamId}
```

### 5.5 添加项目成员

```
POST /api/projects/{projectId}/members/{userId}
```

### 5.6 删除项目 ⚠️ 需管理员或走审批

```
DELETE /api/projects/{projectId}
Authorization: Bearer <token>
```

**权限说明：**
- **团队管理员 / 系统管理员（ROLE_ADMIN）**：直接删除成功
- **普通成员**：后端返回 `403 Forbidden`，提示"只有管理员可以删除项目，请通过审批申请"。前端应引导用户走审批流程（`POST /api/approvals`，targetType=`PROJECT_DELETE`）

**响应：**
```json
{
  "code": 200,
  "message": "项目已删除"
}
```

> 级联删除项目的看板列、任务、成员关系、报告、Sprint 等所有关联数据。

---

## 六、看板模块

### 6.1 获取看板

```
GET /api/board/{projectId}
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "projectId": 1,
    "projectName": "Mimo开发",
    "columns": [
      {
        "id": 1,
        "name": "待办",
        "color": "#909399",
        "sortOrder": 0,
        "issues": [
          {
            "id": 1,
            "issueKey": "MIMO-1",
            "title": "用户登录功能",
            "type": "STORY",
            "priority": "HIGH",
            "status": "TODO",
            "assigneeId": 2,
            "assigneeName": "zhangsan",
            "labels": [
              { "label": "前端", "color": "#409EFF" }
            ]
          }
        ]
      }
    ]
  }
}
```

### 6.2 创建看板列

```
POST /api/board/column
Content-Type: application/json
```

**请求体：**
```json
{
  "projectId": 1,
  "name": "string",
  "color": "#409EFF"
}
```

### 6.3 编辑看板列

```
PUT /api/board/column
Content-Type: application/json
```

**请求体：**
```json
{
  "id": 1,
  "name": "string",
  "color": "string"
}
```

### 6.4 删除看板列

```
DELETE /api/board/column/{id}
```

> 列下有任务时无法删除。

### 6.5 排序列

```
PUT /api/board/column/sort
Content-Type: application/json
```

**请求体：**
```json
[1, 3, 2]
```

### 6.6 拖动任务

```
PUT /api/board/issue/move
Content-Type: application/json
```

**请求体：**
```json
{
  "issueId": 1,
  "targetColumnId": 2,
  "sortOrder": 0
}
```

**后端处理逻辑（v2.6 增强）**：
1. 查询目标看板列信息
2. 根据 **任务类型** 和 **目标列名** 同步更新状态字段：
   - **STORY/TASK**：映射通用 status（待办→TODO、进行中→IN_PROGRESS、完成→DONE）
   - **BUG 类型**：映射 bugStatus，并 **校验状态转换合法性**
3. BUG 状态转换规则验证（不合法则只更新位置不更新状态）：
   - NEW → CONFIRMED / CLOSED
   - CONFIRMED → IN_PROGRESS
   - IN_PROGRESS → RESOLVED
   - RESOLVED → VERIFIED / REOPENED
   - VERIFIED → CLOSED
   - CLOSED → REOPENED
   - REOPENED → IN_PROGRESS
4. 更新 issue 的 columnId + sortOrder + status/bugStatus
5. 记录 MOVE 类型活动日志
6. 通过 WebSocket 向 `/topic/board/{projectId}` 广播同步事件

> 移动任务时后端会自动同步更新状态字段；BUG 类型的状态转换受规则约束，非法转换不会报错而是跳过状态更新。

### 6.7 WebSocket 看板同步

同一看板内的多人协同通过 WebSocket 实时同步，拖拽操作后所有在线用户视图自动更新。

**订阅地址**：`/topic/board/{projectId}`

**事件类型**：

| 类型 | 触发时机 | 携带字段 |
|------|---------|---------|
| `ISSUE_MOVED` | 任务被拖到新列 | issueId, targetColumnId, sortOrder |
| `ISSUE_CREATED` | 新建任务 | issueId, issue（完整 IssueVO） |
| `ISSUE_DELETED` | 删除任务 | issueId |

**事件格式**：
```json
{
  "type": "ISSUE_MOVED",
  "projectId": 1,
  "issueId": 5,
  "targetColumnId": 2,
  "sortOrder": 0
}
```

---

## 七、任务模块

支持三种工作项类型——**故事(STORY)**、**任务(TASK)**、**缺陷(BUG)**，每种类型有差异化字段和功能。

### 7.1 创建任务

```
POST /api/issues
Content-Type: application/json
```

**请求体（TASK 类型）：**
```json
{
  "projectId": 1,
  "columnId": 1,
  "title": "string (必填)",
  "description": "string (可选)",
  "type": "TASK",
  "priority": "HIGHEST | HIGH | MEDIUM | LOW | LOWEST",
  "assigneeId": 2,
  "sprintId": 1,
  "storyPoints": 5,
  "dueDate": "2024-02-01",
  "parentId": 10,
  "labels": ["前端", "核心功能"]
}
```

**请求体（STORY 类型专属字段）：**
```json
{
  "type": "STORY",
  "userRole": "管理员",
  "userGoal": "导出月度报表",
  "businessValue": "分析团队效率趋势",
  "epic": "报表模块",
  "storyPoints": 8
}
```

**请求体（BUG 类型专属字段）：**
```json
{
  "type": "BUG",
  "severity": "BLOCKER | CRITICAL | MAJOR | MINOR | TRIVIAL",
  "stepsToRepro": "1. 打开登录页\n2. 输入账号密码\n3. 点击登录",
  "environment": "Windows 10; Chrome 120; v1.2.0",
  "expectedResult": "正常跳转到首页",
  "actualResult": "页面白屏，控制台报错",
  "foundVersion": "1.2.0",
  "fixedVersion": ""
}
```

任务会自动生成 issueKey（如 `MIMO-1`）。TASK/STORY 初始状态为 `TODO`，BUG 初始 bug_status 为 `NEW`。

**响应（STORY 示例）：**
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "issueKey": "MIMO-1",
    "title": "作为管理员，我希望导出月度报表，以便分析团队效率趋势",
    "type": "STORY",
    "priority": "HIGH",
    "status": "TODO",
    "userRole": "管理员",
    "userGoal": "导出月度报表",
    "businessValue": "分析团队效率趋势",
    "epic": "报表模块",
    "storyPoints": 8,
    "acceptanceCriteria": [
      { "id": "a1b2c3d4", "text": "支持导出 PDF 和 Excel 格式", "done": false }
    ],
    "projectId": 1,
    "labels": [{ "id": 1, "label": "前端", "color": "#409EFF" }],
    "subTasks": [],
    "createdAt": "2024-01-15 10:30:00"
  }
}
```

### 7.2 获取任务详情

```
GET /api/issues/{id}
```

返回完整 IssueVO，含 STORY 的 `acceptanceCriteria`、`subTasks`，BUG 的 `bugStatus`、`environment` 等所有专属字段。

**IssueVO 公共字段（节选）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 任务 ID |
| issueKey | String | 任务编号，如 `MIMO-1` |
| projectId | Long | 所属项目 ID（v2.1+，用于全局搜索跳转、通知深链） |
| title | String | 标题 |
| type | String | STORY / TASK / BUG |
| status | String | TODO / IN_PROGRESS / DONE |
| columnId | Long | 所在看板列 ID |
| assigneeId | Long | 指派人 ID |
| assigneeName | String | 指派人用户名 |

> STORY / BUG 另有专属字段，见 §7.1 请求体说明及操作手册「工作项类型详解」。

### 7.3 更新任务

```
PUT /api/issues
Content-Type: application/json
```

请求体同创建，需包含 `id` 字段。可修改的字段包括：title、description、type、priority、assigneeId、sprintId、columnId、dueDate、storyPoints、severity、stepsToRepro、status，以及各类型专属字段（见 §7.1）。

### 7.4 删除任务

```
DELETE /api/issues/{id}
```

**级联删除**（v2.6 修复）：
1. 删除任务评论（comments 表）
2. 删除任务附件（attachments 表）
3. 删除任务标签（issue_labels 表）
4. 删除相关通知（notifications 表，relatedType='ISSUE'）
5. 解除子任务父子关系（子任务的 parentId 设为 null）
6. 删除任务本身（issues 表）
7. 记录活动日志（action=DELETE）
8. 通过 WebSocket 广播删除事件

> 所有操作在同一事务中执行，任一步骤失败则整体回滚。

**响应：**
```json
{
  "code": 200,
  "message": "删除成功"
}
```

### 7.5 查询任务

```
POST /api/issues/query
Content-Type: application/json
```

**请求体：**
```json
{
  "page": 1,
  "size": 20,
  "projectId": 1,
  "sprintId": 1,
  "assigneeId": 2,
  "type": "STORY",
  "priority": "HIGH",
  "status": "TODO",
  "bugStatus": "NEW",
  "epic": "报表模块",
  "parentId": 10,
  "keyword": "登录"
}
```

所有筛选字段均可选。新增筛选字段：`bugStatus`（BUG 状态）、`epic`（史诗）、`parentId`（父故事 ID）。`keyword` 匹配标题和 issueKey。

**全局搜索用法**：省略 `projectId` 并仅传 `keyword`（及分页参数），可跨用户有权限的项目搜索任务。前端 Command Palette（Ctrl+K）即基于此接口实现。

**响应示例（查询结果单项）：**
```json
{
  "id": 12,
  "issueKey": "MIMO-12",
  "projectId": 1,
  "title": "修复登录超时",
  "type": "BUG",
  "priority": "HIGH",
  "status": "TODO",
  "assigneeName": "zhangsan"
}
```

### 7.6 验收标准管理（STORY 专属）

**新增验收标准：**
```
POST /api/issues/{id}/acceptance-criteria
Content-Type: application/json
```
```json
{ "text": "系统需在 3 秒内响应" }
```

**更新/勾选验收标准：**
```
PUT /api/issues/{id}/acceptance-criteria/{criteriaId}
Content-Type: application/json
```
```json
{ "text": "更新后的文本（可选）", "done": true }
```

**删除验收标准：**
```
DELETE /api/issues/{id}/acceptance-criteria/{criteriaId}
```

以上三个端点均返回最新的验收标准列表 `List<AcceptanceCriterion>`。

### 7.7 缺陷状态管理（BUG 专属）

**变更缺陷状态：**
```
PUT /api/issues/{id}/bug-status
Content-Type: application/json
```
```json
{ "bugStatus": "CONFIRMED" }
```

BUG 状态转换规则（不遵循规则的转换返回 400）：

| 当前状态 | 允许转换到 |
|---------|-----------|
| NEW | CONFIRMED, CLOSED |
| CONFIRMED | IN_PROGRESS |
| IN_PROGRESS | RESOLVED |
| RESOLVED | VERIFIED, REOPENED |
| VERIFIED | CLOSED |
| CLOSED | REOPENED |
| REOPENED | IN_PROGRESS |

### 7.8 AI 辅助功能 ⚠️ v2.8.0 全面升级

> **v2.7.3 变更**: AI 功能从前端直接调用 DeepSeek API 改为通过后端代理，API Key 不再暴露在前端代码中。
> **v2.8.0 升级**: 从2个薄接口扩展为完整 AI Copilot 系统，新增SSE流式对话、智能任务创建、Story拆分、故事点估算。
> **v2.9.0 变更**: SSE 流式端点从 `GET` + URL 参数改为 `POST` + JSON body，避开浏览器 URL 长度限制；后端改用 `AsyncContext` 原生异步 API，彻底消除 406 / ERR_ABORTED。

**环境变量要求**: 后端需配置 `DEEPSEEK_API_KEY`，否则返回"AI 服务未配置"。

#### 7.8.1 SSE 流式对话（核心接口）

```
POST /api/ai/chat/stream
Content-Type: application/json
Authorization: Bearer <token>
Accept: text/event-stream, */*
```

**请求体** (JSON):

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| message | String | 是 | 用户消息内容 |
| systemPrompt | String | 否 | 系统提示词，默认"你是一个项目管理AI助手" |

**响应**: `Content-Type: text/event-stream;charset=UTF-8`，Server-Sent Events 逐条推送 `data: {...}` 行：

```
data: {"content": "你好", "done": false}
data: {"content": "！我是", "done": false}
data: {"content": "Mimo AI助手", "done": false}
data: {"content": "", "done": true}
```

> **实现要点**: 后端使用 `javax.servlet.AsyncContext` 原生异步 API 绕过 Spring MVC content negotiation，前端使用 `fetch` + `ReadableStream` 解析，并通过 `AbortController` 在用户发送新消息时终止上一条流。Vite 代理需配置 `noBuffering` 禁用压缩。

#### 7.8.2 AI 润色描述

```
POST /api/ai/polish
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体：**
```json
{
  "text": "这个功能需要实现用户登录和注册"
}
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "polishedText": "本模块需实现完整的用户身份认证体系，包括用户注册、登录验证及会话管理功能。",
    "originalText": "这个功能需要实现用户登录和注册"
  }
}
```

#### 7.8.3 AI 推荐优先级

```
POST /api/ai/priority
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体：**
```json
{
  "title": "用户无法登录",
  "description": "输入正确密码后提示密码错误"
}
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "suggestedPriority": "HIGH",
    "reason": "影响核心功能可用性，阻碍正常使用"
  }
}
```

#### 7.8.4 AI 解析自然语言为任务结构

```
POST /api/ai/parse-issue
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体：**
```json
{
  "input": "用户反馈登录后首页加载超过5秒，移动端更严重，本周需要解决"
}
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "type": "BUG",
    "priority": "HIGHEST",
    "title": "首页加载超时问题",
    "description": "用户反馈登录后首页加载超过5秒...",
    "severity": "CRITICAL",
    "labels": ["性能", "P0"]
  }
}
```

#### 7.8.5 AI 拆分子任务建议

```
POST /api/ai/suggest-tasks
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体：**
```json
{
  "storyTitle": "作为管理员，我希望导出月度报表",
  "storyDescription": "支持PDF和Excel格式导出"
}
```

**响应：**
```json
{
  "code": 200,
  "data": [
    { "title": "设计报表数据查询接口", "type": "TASK", "priority": "HIGH" },
    { "title": "实现PDF模板渲染", "type": "TASK", "priority": "HIGH" },
    { "title": "实现Excel导出功能", "type": "TASK", "priority": "MEDIUM" }
  ]
}
```

#### 7.8.6 AI 估算故事点

```
POST /api/ai/estimate-story-points
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体：**
```json
{
  "title": "用户身份认证模块",
  "description": "包括注册、登录、JWT令牌管理",
  "acceptanceCriteria": ["支持邮箱+密码注册", "JWT 24小时过期", "支持刷新令牌"]
}
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "storyPoints": 8,
    "reason": "涉及3个验收标准，含安全相关逻辑，复杂度较高"
  }
}
```

#### 7.8.7 查询AI使用量

```
GET /api/ai/usage
Authorization: Bearer <token>
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "totalCalls": 150,
    "totalTokens": 45000,
    "dailyLimit": 1000,
    "dailyUsed": 42,
    "resetAt": "2026-06-17T00:00:00"
  }
}
```

---


## 八、评论模块

### 8.1 创建评论

```
POST /api/comments
Content-Type: application/json
```

**请求体：**
```json
{
  "issueId": 1,
  "content": "这个问题的根因是..."
}
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "issueId": 1,
    "userId": 1,
    "username": "admin",
    "content": "这个问题的根因是...",
    "createdAt": "2024-01-15 14:30"
  }
}
```

### 8.2 获取任务评论列表

```
GET /api/comments/issue/{issueId}
```

按时间倒序排列。

### 8.3 删除评论 ⚠️ v2.7.3 新增

```
DELETE /api/comments/{commentId}
Authorization: Bearer <token>
```

> 仅评论作者可删除自己的评论。非作者返回 403。

**响应：**
```json
{ "code": 200, "message": "评论已删除" }
```

---

## 九、通知模块

### 9.1 获取通知列表

```
GET /api/notifications?limit=20
```

**响应：**
```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "type": "ASSIGNED",
      "title": "新任务分配",
      "content": "你被分配了任务 MIMO-1: 用户登录功能",
      "relatedId": 1,
      "relatedType": "ISSUE",
      "isRead": 0,
      "createdAt": "2024-01-15 10:30"
    },
    {
      "id": 2,
      "type": "APPROVAL_APPROVED",
      "title": "审批已通过",
      "content": "您的审批请求「创建项目: Mimo开发」已被管理员通过",
      "relatedId": 5,
      "relatedType": "APPROVAL_REQUEST",
      "isRead": 0,
      "createdAt": "2024-01-15 11:00"
    },
    {
      "id": 3,
      "type": "APPROVAL_REJECTED",
      "title": "审批被拒绝",
      "content": "您的审批请求「删除项目: 测试项目」被管理员拒绝，原因：项目正在使用中",
      "relatedId": 6,
      "relatedType": "APPROVAL_REQUEST",
      "isRead": 0,
      "createdAt": "2024-01-15 12:00"
    }
  ]
}
```

**通知类型说明（v2.7.2 新增审批通知）：**

| type | 触发场景 | relatedType |
|------|---------|-------------|
| `ASSIGNED` | 任务被指派给当前用户 | `ISSUE` |
| `STATUS_CHANGED` | 指派任务的状态变更 | `ISSUE` |
| **`APPROVAL_APPROVED`** (v2.7.2) | 审批请求**通过**，自动写入 notifications 表 | `APPROVAL_REQUEST` |
| **`APPROVAL_REJECTED`** (v2.7.2) | 审批请求**拒绝**，含拒绝原因 | `APPROVAL_REQUEST` |

> 审批通知由后端 `ApprovalService.approve()` / `reject()` 自动触发，通过 `NotificationService.create()` 写入数据库。前端登录后通过 `GET /api/notifications` 获取并弹窗提示。

### 9.2 获取未读通知数

```
GET /api/notifications/unread-count
```

**响应：** `{ "code": 200, "data": { "count": 3 } }`

### 9.3 标记单条已读

```
PUT /api/notifications/{id}/read
```

### 9.4 全部标记已读

```
PUT /api/notifications/read-all
```

### 9.5 WebSocket 实时推送

除 HTTP 接口外，通知支持通过 WebSocket 实时推送，前端无需轮询。客户端连接后订阅个人频道即可收到即时推送。

**连接端点**：`/api/ws`（SockJS，兼容非 WebSocket 环境）

**认证**：CONNECT 帧携带 `Authorization: Bearer <token>` 头

**订阅地址**：`/topic/notifications/{userId}`

**推送格式**：
```json
{
  "type": "ASSIGNED | STATUS_CHANGED",
  "title": "通知标题",
  "content": "通知内容",
  "relatedId": 1,
  "relatedType": "ISSUE"
}
```

> 触发场景与原 HTTP 通知一致（任务分配、状态变更、新评论）。

---


---

## 十、统计报表模块

### 10.1 获取项目统计数据

```
GET /api/reports/stats/{projectId}
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "weeklyCompleted": [
      { "date": "01-15", "count": 3 },
      { "date": "01-16", "count": 5 }
    ],
    "memberDistribution": [
      { "username": "admin", "count": 8 },
      { "username": "zhangsan", "count": 5 }
    ],
    "sprintVelocity": [
      { "sprintName": "Sprint 1", "totalPoints": 5, "completedPoints": 3 }
    ]
  }
}
```

---

## 十一、Sprint 模块

### 11.1 创建 Sprint

```
POST /api/sprints
Content-Type: application/json
```

**请求体：**
```json
{
  "projectId": 1,
  "name": "Sprint 1",
  "goal": "完成用户模块",
  "startDate": "2024-01-15",
  "endDate": "2024-01-28"
}
```

初始状态为 `PLANNING`。

### 11.2 获取 Sprint

```
GET /api/sprints/{id}
```

### 11.3 项目的 Sprint 列表

```
GET /api/sprints/project/{projectId}
```

**响应：**
```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "name": "Sprint 1",
      "goal": "完成用户模块",
      "startDate": "2024-01-15",
      "endDate": "2024-01-28",
      "status": "ACTIVE",
      "isActive": 1,
      "totalIssues": 8,
      "completedIssues": 4,
      "projectName": "Mimo开发"
    }
  ]
}
```

### 11.4 启动 Sprint

```
PUT /api/sprints/{id}/start
```

状态从 `PLANNING` → `ACTIVE`。自动停用同项目其他活跃 Sprint，并生成首日燃尽快照。

### 11.5 完成 Sprint

```
PUT /api/sprints/{id}/complete
```

状态从 `ACTIVE` → `COMPLETED`。

### 11.6 一键创建迭代

```
POST /api/sprints/quick/{projectId}
```

自动创建 2 周迭代（从今天开始），状态设为 ACTIVE，并将看板待办列前 5 个 TODO 任务自动加入该 Sprint。

### 11.7 完成 Sprint 并迁移任务

```
PUT /api/sprints/{id}/complete-migration?targetSprintId=2
```

完成 Sprint 时处理未完成任务：指定 `targetSprintId` 则移至目标 Sprint，否则移回看板待办列。

### 11.8 将任务加入 Sprint

```
PUT /api/sprints/{issueId}/add-to-sprint/{sprintId}
```

### 11.9 生成燃尽快照 ⚠️ v2.7.3 新增

```
POST /api/sprints/{sprintId}/snapshot
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体（可选）：**
```json
{
  "doneIssues": 3,
  "remainingIssues": 5
}
```

手动生成 Sprint 燃尽快照数据点。

### 11.10 完成 Sprint 迁移 ⚠️ v2.7.3 新增

```
PUT /api/sprints/{sprintId}/complete-migration
Authorization: Bearer <token>
```

完成当前 Sprint 并将未完成任务迁移到下一 Sprint 或看板待办列。

---

## 十二、燃尽图模块

### 12.1 获取燃尽图数据

```
GET /api/sprints/{id}/burndown
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "sprintName": "Sprint 1",
    "startDate": "2024-01-15",
    "endDate": "2024-01-28",
    "totalPoints": 40,
    "points": [
      {
        "date": "2024-01-15",
        "idealRemaining": 40.0,
        "actualRemaining": 40
      },
      {
        "date": "2024-01-16",
        "idealRemaining": 37.0,
        "actualRemaining": 35
      }
    ]
  }
}
```

### 12.2 生成燃尽快照

```
POST /api/sprints/{id}/snapshot
```

手动触发一次快照计算（通常在启动 Sprint 时自动生成）。

### 12.3 Sprint 成员统计

```
GET /api/sprints/{id}/stats
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "sprintId": 1,
    "sprintName": "Sprint 1",
    "totalIssues": 10,
    "completedIssues": 6,
    "overallCompletionRate": 60.0,
    "overallOverdueRate": 20.0,
    "memberStats": [
      {
        "username": "zhangsan",
        "assigneeId": 2,
        "totalAssigned": 5,
        "completed": 4,
        "overdue": 1,
        "completionRate": 80.0,
        "avgDaysInColumn": 2.33
      }
    ]
  }
}
```

| 字段 | 说明 |
|------|------|
| `overallCompletionRate` | 整体完成率（百分比） |
| `overallOverdueRate` | 整体超期率（dueDate 过期且未完成 / 总任务数） |
| `memberStats[].completionRate` | 按成员统计的完成率 |
| `memberStats[].overdue` | 该成员超期未完成任务数 |
| `memberStats[].avgDaysInColumn` | 平均每列停留天数（估算流程效率） |

---

## 十四、工时记录模块（v2.9.2 新增）

工时记录用于追踪每个 Issue 上花费的实际工时（小时），支持团队工时汇总与 Sprint 维度工作量分析。

### 14.1 创建工时记录

```
POST /api/worklogs
```

**请求体：**
```json
{
  "issueId": 1,
  "workDate": "2026-06-18",
  "hours": 1.5,
  "description": "完成登录模块联调"
}
```

| 字段 | 必填 | 说明 |
|------|------|------|
| `issueId` | ✅ | 关联 Issue ID |
| `workDate` | ✅ | 工时日期 (YYYY-MM-DD) |
| `hours` | ✅ | 工时小时数，0.01 ~ 24 |
| `description` | ❌ | 工时说明（≤200 字） |

**响应**：`WorkLogVO`（见 14.2）

### 14.2 列出某 Issue 的工时

```
GET /api/worklogs/issue/{issueId}
```

**响应**：`WorkLogVO[]`（按日期倒序）

```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "issueId": 1,
      "issueKey": "MIMO-1",
      "issueTitle": "登录模块",
      "userId": 2,
      "username": "zhangsan",
      "avatar": "...",
      "workDate": "2026-06-18",
      "hours": 1.5,
      "description": "完成登录模块联调",
      "createdAt": "2026-06-18 14:30:00"
    }
  ]
}
```

### 14.3 删除工时记录

```
DELETE /api/worklogs/{id}
```

权限：创建人或团队管理员可删除。

### 14.4 累计工时

```
GET /api/worklogs/issue/{issueId}/sum
```

**响应**：`number`（如 `3.50`，单位：小时）

### 14.5 Sprint 工时汇总

```
GET /api/worklogs/sprint/{sprintId}/summary
```

**响应：**

```json
{
  "code": 200,
  "data": {
    "sprintId": 1,
    "sprintName": "Sprint 12",
    "totalHours": 12.50,
    "memberCount": 3,
    "members": [
      {
        "userId": 2,
        "username": "zhangsan",
        "avatar": "...",
        "totalHours": 6.0,
        "logCount": 4,
        "issueCount": 3
      }
    ]
  }
}
```

| 字段 | 说明 |
|------|------|
| `totalHours` | 该成员在 Sprint 下累计工时 |
| `logCount` | 工时记录数 |
| `issueCount` | 涉及 Issue 数（去重） |

---

## 十三、报告模块

### 13.1 生成报告

```
POST /api/reports/generate
Content-Type: application/json
```

**请求体：**
```json
{
  "projectId": 1,
  "type": "DAILY | WEEKLY",
  "reportDate": "2024-01-20"
}
```

自动统计已完成任务生成报告内容，初始状态为 `DRAFT`。

### 13.2 报告列表

```
GET /api/reports?type=DAILY&projectId=1
```

参数均可选。

### 13.3 获取报告

```
GET /api/reports/{id}
```

### 13.4 更新报告

```
PUT /api/reports
Content-Type: application/json
```

**请求体：**
```json
{
  "id": 1,
  "content": "markdown 格式内容"
}
```

仅 `DRAFT` 状态的报告可编辑。

### 13.5 提交报告

```
PUT /api/reports/{id}/submit
```

状态从 `DRAFT` → `SUBMITTED`。提交后不可再编辑。

---

## 十四、附件模块

### 14.1 上传附件

```
POST /api/attachments/upload
Content-Type: multipart/form-data
```

> **注意**：这是唯一使用 multipart/form-data 格式的接口，不要用 application/json。

**表单格式：**

| 字段 | 类型 | 说明 |
|------|------|------|
| issueId | number | 任务 ID |
| file | file | 文件（最大 10MB） |

### 14.2 任务附件列表

```
GET /api/attachments/issue/{issueId}
```

**响应：**
```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "fileName": "screenshot.png",
      "fileSize": 204800,
      "uploaderName": "admin",
      "createdAt": "2024-01-20 14:00:00"
    }
  ]
}
```

### 14.3 下载附件

```
GET /api/attachments/{id}/download
```

返回文件流，浏览器自动下载。

### 14.4 删除附件

```
DELETE /api/attachments/{id}
```

---

## 十五、团队群聊模块

> v2.2.0 新增，v2.4.0 增强 Emoji 支持，v2.5.0 新增消息撤回与 @提及。团队内实时群聊功能，支持消息持久化存储与 WebSocket 实时推送。前端内置 4 类 Emoji 选择器（表情/手势/物品/符号，200+ 个），支持光标位置插入；输入 `@` 触发成员提及选择器；发送者可在 2 分钟内撤回消息。

### 15.1 发送消息

```
POST /api/chat/send
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体：**
```json
{
  "teamId": 1,
  "content": "大家好，今天的任务进展如何？"
}
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "teamId": 1,
    "senderId": 1,
    "senderName": "admin",
    "senderAvatar": "",
    "content": "大家好，今天的任务进展如何？",
    "createdAt": "2026-06-10 11:30:00"
  }
}
```

发送成功后，消息会通过 WebSocket 广播给该团队所有在线成员。

### 15.2 获取聊天历史

```
GET /api/chat/history/{teamId}
Authorization: Bearer <token>
```

**响应：**
```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "teamId": 1,
      "senderId": 1,
      "senderName": "admin",
      "senderAvatar": "",
      "content": "大家好，今天的任务进展如何？",
      "createdAt": "2026-06-10 11:30:00"
    }
  ]
}
```

返回最近 100 条消息，按时间正序排列。

### 15.3 WebSocket 实时接收消息

**订阅地址**：`/topic/team-chat/{teamId}`

**连接端点**：`/api/ws`（SockJS + STOMP）

**认证**：CONNECT 帧携带 `Authorization: Bearer <token>` 头

**事件格式（CHAT_MESSAGE）：**
```json
{
  "type": "CHAT_MESSAGE",
  "teamId": 1,
  "message": {
    "id": 1,
    "teamId": 1,
    "senderId": 1,
    "senderName": "admin",
    "senderAvatar": "",
    "content": "大家好，今天的任务进展如何？",
    "createdAt": "2026-06-10 11:30:00"
  }
}
```

前端收到 `CHAT_MESSAGE` 事件后，将 `message` 追加到消息列表并自动滚动到底部。根据 `senderId` 判断是否为当前用户发送的消息（用于气泡左右对齐）。

**事件格式（CHAT_RECALL — 消息撤回）：**
```json
{
  "type": "CHAT_RECALL",
  "teamId": 1,
  "messageId": 42
}
```

前端收到 `CHAT_RECALL` 事件后，将对应消息标记为已撤回状态（显示灰色斜体"消息已撤回"）。

### 15.4 撤回消息

> v2.5.0 新增。仅消息发送者可在 **2 分钟内** 撤回自己发送的消息。

```
PUT /api/chat/recall/{messageId}
Authorization: Bearer <token>
```

**响应（成功）：**
```json
{
  "code": 200,
  "message": "消息已撤回"
}
```

**错误场景：**

| 场景 | 响应 |
|------|------|
| 消息不存在 | 404 "消息不存在" |
| 非本人发送 | 403 "只能撤回自己的消息" |
| 超过 2 分钟 | 400 "已超过撤回时间限制(2分钟)" |
| 已被撤回过 | 200 (幂等) |

撤回成功后：
1. 后端更新 `chat_messages.recalled = true`
2. 通过 WebSocket 向 `/topic/team-chat/{teamId}` 广播 `CHAT_RECALL` 事件

### 15.5 @提及功能

> v2.5.0 新增。在聊天输入框中输入 `@` 字符时，自动弹出团队成员选择面板（支持模糊搜索，最多显示 8 条）。选择成员后，在光标位置插入 `@username ` 格式文本。

**前端行为：**
- 输入 `@` → 弹出成员列表 Popover（过滤当前已 @ 的成员）
- 点击成员 → 插入 `@username ` 到输入框光标位置
- 消息渲染时，正则匹配 `@\w+` 替换为绿色高亮 `<span class="mention">@username</span>`

---

## 十六、日历模块（v2.8.0 上线，v2.9.0 增强）

> **v2.8.0**: 月/周/日视图 + 事件 CRUD + 任务截止日期自动同步
> **v2.9.0**: 团队可见性 + 项目关联 + 团队/事件类型筛选

日历事件支持四种来源：
1. 用户手动创建（标题、描述、起止时间、提醒）
2. 任务截止日期（`eventType=TASK_DEADLINE`，自动同步）
3. Sprint 起止（`eventType=SPRINT`）
4. 会议/提醒（`eventType=MEETING` / `REMINDER`）

**可见性规则**:
- 创建者本人可看自己的所有事件
- 标记了 `teamId` 的事件，所有该团队成员可见
- 标记了 `projectId` 的事件，所有该项目的成员可见
- 无 teamId/projectId 的事件仅创建者私有

**数据库**: `calendar_events` 表，迁移脚本 V6（基础字段）+ V7（team_id 字段）

#### 16.1 创建事件

```
POST /api/calendar
Authorization: Bearer <token>
Content-Type: application/json
```

**请求体**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| teamId | Long | 否 | 关联团队，设为团队内可见 |
| projectId | Long | 否 | 关联项目，设为项目内可见 |
| title | String | 是 | 事件标题 |
| description | String | 否 | 详细描述 |
| startTime | LocalDateTime | 是 | 开始时间（ISO 8601） |
| endTime | LocalDateTime | 是 | 结束时间（ISO 8601） |
| allDay | Boolean | 否 | 是否全天事件 |
| eventType | String | 否 | TASK_DEADLINE / MEETING / REMINDER / SPRINT / CUSTOM |
| relatedId | Long | 否 | 关联对象 ID（任务/Sprint） |
| relatedType | String | 否 | ISSUE / SPRINT |
| color | String | 否 | 显示颜色（HEX） |
| location | String | 否 | 地点 |
| participants | List&lt;Long&gt; | 否 | 参与人员 ID 列表 |
| reminderMinutes | Integer | 否 | 提前提醒分钟数 |

**响应**: `Result<EventVO>`

#### 16.2 更新事件

```
PUT /api/calendar/{id}
Authorization: Bearer <token>
Content-Type: application/json
```

请求体同创建。**权限**: 仅创建者本人可改。

#### 16.3 删除事件

```
DELETE /api/calendar/{id}
Authorization: Bearer <token>
```

**权限**: 仅创建者本人可删。

#### 16.4 查询事件列表（核心接口）

```
GET /api/calendar?start=2026-06-01T00:00:00&end=2026-07-01T00:00:00
    &teamId=1&projectId=2&eventType=MEETING
Authorization: Bearer <token>
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| start | LocalDateTime | 是 | 查询起始时间 |
| end | LocalDateTime | 是 | 查询结束时间 |
| teamId | Long | 否 | 限定团队范围 |
| projectId | Long | 否 | 限定项目范围 |
| eventType | String | 否 | 限定事件类型 |

**响应**: `Result<List<EventVO>>`，按 `startTime ASC` 排序。

**自动同步**: 当任务的 `dueDate` 变更时，CalendarSyncService 会自动增删改 `calendar_events` 中对应 `eventType=TASK_DEADLINE, relatedType=ISSUE` 的事件。

#### 16.5 查询单个事件

```
GET /api/calendar/{id}
Authorization: Bearer <token>
```

**响应**: `Result<EventVO>`

---

---

## 十三、审批流程模块（v2.7 新增）

### 13.1 创建审批请求

```
POST /api/approvals
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体：**
```json
{
  "teamId": 1,
  "projectId": 2,
  "targetType": "PROJECT_CREATE",
  "title": "创建项目：新功能开发",
  "description": "申请创建新项目用于...",
  "dataJson": { "name": "新项目", "key": "NEW" }
}
```

**targetType 可选值：**
| 值 | 说明 |
|----|------|
| PROJECT_CREATE | 创建项目 |
| PROJECT_UPDATE | 修改项目 |
| PROJECT_DELETE | 删除项目 |
| PROJECT_MEMBER_ADD | 添加成员 |

**响应（成功）：**
```json
{
  "code": 200,
  "message": "审批请求已提交，等待管理员审核",
  "data": {
    "id": 1,
    "status": "PENDING"
  }
}
```

### 13.2 查询团队待审批列表

```
GET /api/approvals/team/{teamId}/pending
Authorization: Bearer <token>
```

**响应：**
```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "teamId": 1,
      "projectId": 2,
      "requesterId": 3,
      "requesterName": "lisi",
      "targetType": "PROJECT_CREATE",
      "title": "创建项目：新功能开发",
      "description": "申请创建新项目用于...",
      "status": "PENDING",
      "createdAt": "2026-06-12 10:30:00"
    }
  ]
}
```

> 仅团队成员中的管理员可调用此接口。

### 13.2b 查询所有待审批列表（仅系统admin）⚠️ v2.7.1 新增

```
GET /api/approvals/pending
Authorization: Bearer <token>
```

**权限说明**：仅系统管理员（ROLE_ADMIN）可调用。返回所有团队的待审批请求（不限于单个团队），适用于 admin 左侧「审批管理」页面。

**响应格式同 13.2**，数据范围为全系统。

### 13.3 查询我的审批请求

```
GET /api/approvals/my
Authorization: Bearer <token>
```

返回当前用户发起的所有审批请求（含已通过/已拒绝）。

### 13.4 通过审批

```
PUT /api/approvals/{id}/approve
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体：**
```json
{}
```

**响应：**
```json
{
  "code": 200,
  "message": "审批已通过"
}
```

> 通过后系统会自动执行原操作（如创建/修改/删除项目）。

### 13.5 拒绝审批

```
PUT /api/approvals/{id}/reject?reason=拒绝原因
Authorization: Bearer <token>
```

**请求体：** 无（reason 通过 query 参数传递）

**响应：**
```json
{
  "code": 200,
  "message": "审批已拒绝"
}
```

### 13.6 撤回审批请求

```
PUT /api/approvals/{id}/withdraw
Authorization: Bearer <token>
```

申请人可撤回自己提交的、仍处于 PENDING 状态的审批请求。撤回后状态变为 `WITHDRAWN`。

**响应：**
```json
{
  "code": 200,
  "message": "审批请求已撤回"
}
```

### 13.7 清理过期审批请求

```
POST /api/approvals/cleanup-expired
Authorization: Bearer <token>
```

清理超过 7 天仍处于 PENDING 状态的审批请求，将其标记为 `EXPIRED`。

**响应：**
```json
{
  "code": 200,
  "data": { "cleanedCount": 2 }
}
```

---

## 十四、用户等级模块（v2.7 新增）

### 14.1 获取当前用户等级

```
GET /api/user-levels/my
Authorization: Bearer <token>
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "userId": 1,
    "level": 4,
    "levelName": "L4",
    "badgeColor": "#E6A23C",
    "badgeIcon": "crown"
  }
}
```

### 14.2 获取所有用户等级列表（仅管理员）

```
GET /api/user-levels/list
Authorization: Bearer <token>
```

### 14.3 获取指定用户等级 ⚠️ v2.7.3 新增

```
GET /api/user-levels/{userId}
Authorization: Bearer <token>
```

获取指定用户的等级信息。无等级记录的用户默认返回 L1。

**响应：**
```json
{
  "code": 200,
  "data": {
    "userId": 2,
    "username": "zhangsan",
    "level": 1,
    "levelName": "L1",
    "badgeColor": "#909399",
    "badgeIcon": null
  }
}
```

### 14.4 修改用户等级（仅管理员）

```
PUT /api/user-levels/{userId}/level
Content-Type: application/json
Authorization: Bearer <token>
```

**请求体：**
```json
{
  "level": 3
}
```

**响应：**
```json
{
  "code": 200,
  "message": "等级已更新为 L3"
}
```

**等级配置说明：**

| 等级 | levelName | badgeColor | badgeIcon | UI 特效 |
|------|-----------|------------|-----------|---------|
| 1 | L1 | #909399 | null | 基础灰色样式 |
| 2 | L2 | #67C23A | star | 绿色 + ⭐ 图标 + 微光 |
| 3 | L3 | #409EFF | medal | 蓝色 + 🏅 图标 + 光晕边框 |
| 4 | L4 | #E6A23C | crown | 金红渐变 + 👑 图标 + 旋转光效 |

---

## 附录 A：枚举值说明

### 任务类型（type）
| 值 | 说明 |
|----|------|
| STORY | 用户故事——包含用户角色、目标功能、业务价值、验收标准 |
| TASK | 任务——可挂载到 STORY 下作为子任务 |
| BUG | 缺陷——独立状态流和缺陷报告要素 |

### 优先级（priority）
| 值 | 级别 |
|----|------|
| HIGHEST | 最高 |
| HIGH | 高 |
| MEDIUM | 中 |
| LOW | 低 |
| LOWEST | 最低 |

### 任务状态（status）— STORY/TASK 共用
| 值 | 说明 |
|----|------|
| TODO | 待办 |
| IN_PROGRESS | 进行中 |
| DONE | 已完成 |

### BUG 状态（bugStatus）— BUG 专属
| 值 | 说明 |
|----|------|
| NEW | 新建——缺陷刚被报告 |
| CONFIRMED | 已确认——经验证确认为有效缺陷 |
| IN_PROGRESS | 修复中——开发人员正在修复 |
| RESOLVED | 已解决——修复完成，等待验证 |
| VERIFIED | 已验证——测试人员确认修复有效 |
| CLOSED | 已关闭——缺陷生命周期结束 |
| REOPENED | 重新打开——验证不通过或再次出现 |

### Bug 严重程度（severity）
| 值 | 说明 |
|----|------|
| BLOCKER | 阻塞——系统无法使用 |
| CRITICAL | 严重——核心功能不可用 |
| MAJOR | 主要——功能严重受损 |
| MINOR | 次要——轻微问题 |
| TRIVIAL | 轻微——建议性改进 |

### Sprint 状态
| 值 | 说明 |
|----|------|
| PLANNING | 规划中 |
| ACTIVE | 进行中 |
| COMPLETED | 已完成 |

### 报告类型
| 值 | 说明 |
|----|------|
| DAILY | 日报 |
| WEEKLY | 周报 |

### 报告状态
| 值 | 说明 |
|----|------|
| DRAFT | 草稿 |
| SUBMITTED | 已提交 |

### 项目模板
| 值 | 说明 |
|----|------|
| SCRUM | 含 Sprint 规划 |
| KANBAN | 看板连续交付 |

### 用户角色
| 值 | 说明 |
|----|------|
| ROLE_ADMIN | 管理员 |
| ROLE_MEMBER | 成员 |

### Story 专属字段说明
| 字段 | 类型 | 说明 |
|------|------|------|
| userRole | VARCHAR(50) | 用户角色（"作为..." 部分） |
| userGoal | VARCHAR(500) | 目标功能（"我希望..." 部分） |
| businessValue | VARCHAR(500) | 业务价值（"以便..." 部分） |
| acceptanceCriteria | JSON | 验收标准列表 `[{"id":"...","text":"...","done":false}]` |
| epic | VARCHAR(100) | 所属史诗/特性分组 |
| parentId | BIGINT | 父级 ID（仅 TASK 使用，指向 STORY） |

### Bug 专属字段说明
| 字段 | 类型 | 说明 |
|------|------|------|
| severity | VARCHAR(20) | 严重程度 |
| stepsToRepro | TEXT | 复现步骤 |
| bugStatus | VARCHAR(20) | 缺陷生命周期状态 |
| environment | VARCHAR(500) | 环境信息（OS/浏览器/版本） |
| expectedResult | TEXT | 期望结果 |
| actualResult | TEXT | 实际结果 |
| foundVersion | VARCHAR(50) | 发现版本 |
| fixedVersion | VARCHAR(50) | 修复版本 |

---

## 附录 B：测试账号

| 用户名 | 密码 | 角色 | 等级 |
|--------|------|------|------|
| admin | 123456 | ROLE_ADMIN | L4 |
| zhangsan | 123456 | ROLE_MEMBER | L1 |
| lisi | 123456 | ROLE_MEMBER | L3 |

---

> 文档版本：v2.9.2 — 工时记录系统 (work_logs 表 + 5 接口 + Issue 工时 Tab + Sprint 工时汇总) (2026-06-18)
