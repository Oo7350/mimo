# Mimo 项目功能完善计划 v2

> 基于项目现状(v2.9.1)与竞品(钉钉/飞书/禅道/Jira)对比分析
> 创建时间: 2026-06-16，最近更新: 2026-06-18 (v2.9.1 Sprint 燃尽图前端可视化)

---

## 一、项目现状盘点

### 已有核心功能

| 模块 | 具体能力 | 成熟度 |
|------|---------|--------|
| 用户系统 | 注册/登录/JWT/用户等级(L1-L4)/单设备登录 | 完善 |
| 团队管理 | 创建团队/成员管理/团队聊天(@提及/撤回) | 完善 |
| 项目管理 | Scrum/Kanban模板/项目概览(ECharts图表) | 较好 |
| 看板 | 3列拖拽/泳道/筛选搜索/实时同步 | 完善 |
| 任务系统 | Story/Task/Bug三类型/评论(Markdown)/附件/标签 | 完善 |
| Sprint | 迭代创建/任务分配/完成迁移/**燃尽图可视化 (v2.9.1)** | **前端完善** |
| 报告 | 日报/周报/PDF导出/ECharts图表 | 较好 |
| 审批流程 | 非管理员操作审批/全局审批列表 | 完善 |
| **AI助手** | **SSE流式对话 (v2.9.0 改用 AsyncContext + POST, 已稳定) + 智能创建 + Story拆分 + 7个接口** | **基础可用** |
| **日历模块** | **月/周/日视图 + 事件 CRUD + 任务 dueDate 自动同步 (v2.8.0) + 团队可见性 + 项目关联 + 团队/类型筛选 (v2.9.0)** | **基础可用** |
| 其他 | 命令面板(Ctrl+K)/暗黑模式/引导教程/通知推送 | 良好 |

### 关键缺失（按影响排序）

| 缺失项 | 影响 | 竞品对比 |
|--------|------|---------|
| **AI上下文能力** | 缺项目/任务上下文注入，需手动指定 | Jira AI / 飞书智能助手 |
| **甘特图** | 时间维度只有日历，缺任务依赖甘特 | 全员标配 |
| 工时追踪 | 无法量化工作量 | 企业必备 |
| 可配置工作流 | 状态机固定3态死板 | Jira核心优势 |
| 细粒度权限 | 仅Admin/Member两级 | 企业门槛 |
| Wiki文档 | 无知识沉淀能力 | Confluence级别需求 |
| ~~Sprint燃尽图~~ | ~~后端有数据但前端无可视化~~ (v2.9.1 已补) | ~~敏捷基本要求~~ |

---

## 二、AI 能力增强专项（Phase 0 — v2.8.0 基础完成，持续迭代）

> **v2.8.0 进度**: 已实现基础框架（后端ai模块 + 7个接口 + 3个前端组件），流式对话存在环境兼容性问题待解决
> **当前状态**: AI仅有 `polish` 和 `priority` 两个薄接口 → **已升级为完整 AI Copilot**

### 2.1 智能任务创建（AI 原生输入）✅ v2.8.0 已实现

**现状**: ~~用户手动填写标题、描述、优先级等字段~~ → **已支持AI对话式创建**
**目标**: 自然语言 → 结构化任务

```
已实现内容 (v2.8.0):
├── 后端 AiController 接口:
│   ├── POST /api/ai/parse-issue          ✅ 自然语言解析为任务结构
│   ├── POST /api/ai/suggest-tasks        ✅ 从Story拆分子任务建议
│   └── POST /api/ai/estimate-story-points ✅ AI估算故事点
│
├── 前端已实现:
│   ├── components/issue/AiIssueCreator.vue    ✅ AI对话式创建任务入口
│   └── components/issue/AiTaskSplitter.vue    ✅ Story拆分面板
│
└── 用户体验:
    ✅ 看板页 "AI 创建" 按钮 → 对话框输入自然语言
    例: "用户登录后页面白屏，Chrome复现，紧急"
    → 自动填充: type=BUG, priority=HIGHEST, severity=BLOCKER,
                description含环境信息模板, suggestAssignee=前端负责人
```

**AI Prompt 设计示例**:

```json
// POST /api/ai/parse-issue 请求
{
  "input": "用户反馈登录后首页加载超过5秒，移动端更严重，本周需要解决",
  "projectId": 1,
  "context": {
    "teamMembers": ["张三(前端)", "李四(后端)", "王五(测试)"],
    "activeSprint": "Sprint 3",
    "labels": ["性能", "移动端", "P0"]
  }
}

// 响应
{
  "title": "优化首页加载性能，目标<3秒",
  "type": "TASK",
  "priority": "HIGH",
  "assigneeSuggestion": "张三(前端)",
  "storyPoints": 5,
  "labels": ["性能", "P0"],
  "description": "## 背景\n用户反馈首页加载缓慢...\n\n## 验收标准\n- [ ] 首屏加载 < 3秒\n- [ ] 移动端首屏 < 2.5秒\n- [ ] Lighthouse Performance > 80",
  "dueDateSuggestion": "2026-06-23",  // AI根据"本周"推断
  "subTaskSuggestions": [
    { "title": "分析当前性能瓶颈(Lighthouse/DevTools)", "type": "TASK", "estimate": 2 },
    { "title": "图片懒加载+CDN优化", "type": "TASK", "estimate": 3 },
    { "title": "移动端专项优化", "type": "TASK", "estimate": 2 },
    { "title": "性能回归测试", "type": "TASK", "estimate": 1 }
  ],
  "confidence": 0.87,
  "disclaimer": "建议人工审核后保存"
}
```

### 2.2 智能 Sprint 规划助手

**现状**: Sprint 手动创建、手动选任务
**目标**: AI 分析 Backlog → 推荐 Sprint 计划

```
新增内容:
├── 后端:
│   ├── POST /api/ai/suggest-sprint-plan     // AI推荐Sprint任务组合
│   ├── POST /api/ai/analyze-sprint-health   // Sprint健康度分析
│   └── POST /api/ai/predict-velocity        // 基于历史预测团队速度
│
├── 前端:
│   ├── views/SprintPlanningView.vue         // Sprint规划向导(替代空白的Sprint管理)
│   └── components/sprint/AiSprintAdvisor.vue // AI建议面板
│
└── 功能场景:
    ├── "帮我们规划下一个2周Sprint" 
    │   → 分析Backlog优先级+团队容量+依赖关系
    │   → 推荐任务列表+理由+风险提示
    ├── "这个Sprint能完成吗?"
    │   → 当前进度 vs 剩余时间 vs 剩余工作量
    │   → 给出调整建议(缩减范围/延期/加人)
    └── "我们团队的Velocity大概是多少?"
        → 历史Sprint完成率趋势图 + 预测值
```

### 2.3 AI 日报/周报生成增强

**现状**: 模板化日报，内容较机械
**目标**: AI 结合实际工作数据生成个性化报告

```
改造内容:
├── 后端 ReportService + AiController:
│   ├── POST /api/ai/generate-daily-report   // 基于当日活动自动生成
│   ├── POST /api/ai/generate-weekly-report  // 周报汇总+下周计划建议
│   └── POST /api/ai/generate-retrospective  // Sprint回顾总结(AI提炼)
│
├── 数据源整合:
│   ├── 当日完成的任务 (from issues)
│   ├── 当日工时记录 (from time_logs, Phase1新增)
│   ├── 当日评论/协作活动 (from comments)
│   ├── 当日Git提交 (未来可集成)
│   └── Blocker/风险项识别
│
└── 输出效果:
    └── 今日完成: 完成3个任务(登录优化[STORY-12]、Bug修复[BUG-8]...)
      进行中: API对接[TASK-15](进度60%)
      明日计划: AI基于优先级+依赖推荐
      风险提示: BUG-8关联的TEST-3尚未开始，可能阻塞周五发布
```

### 2.4 AI 智能问答与知识检索（RAG）

**现状**: 无知识库能力
**目标**: AI 能回答项目相关问题

```
新增内容:
├── 后端:
│   ├── POST /api/ai/chat                     // 项目上下文对话
│   ├── service/RagService.java               // RAG检索增强生成
│   └── 向量存储 (可用MySQL JSON或接入外部向量DB)
│
├── 知识来源:
│   ├── Wiki文档 (Phase 2新增)
│   ├── 任务描述+评论
│   ├── 日报/周报
│   ├── Sprint目标+回顾
│   └── 操作手册/规范文档
│
├── 前端:
│   ├── components/AiChatPanel.vue            // 浮动AI助手面板(右下角)
│   └── 支持在任意页面唤起
│
└── 使用场景:
    ├── "这个项目的架构是什么?" → 从Wiki+代码注释检索回答
    ├── "BUG-45解决了没?" → 查询任务状态+最新评论
    ├── "上周我们完成了什么?" → 汇总上周活动
    ├── "谁最熟悉支付模块?" → 分析任务指派+评论活跃度
    └── "帮我写个登录测试用例" → 基于现有Story生成
```

### 2.5 AI 代码审查助手（未来扩展）

```
预留接口:
├── POST /api/ai/review-code                 // 代码审查(Git webhook触发)
├── POST /api/ai/generate-test-cases          // 根据Story生成测试用例
├── POST /api/ai/explain-error               // 错误日志分析
└── POST /api/ai/refactor-suggestion          // 代码重构建议
```

### 2.6 AI 基础设施升级

```
改造内容:
├── 后端架构升级:
│   ├── 新增 AiProvider 接口 (支持多模型切换):
│   │   ├── DeepSeekImpl (当前)
│   │   ├── OpenAICompatImpl (兼容OpenAI格式)
│   │   ├── QwenImpl (通义千问)
│   │   └── 本地OllamaImpl (离线方案)
│   ├── AiTokenUsageService (用量统计/配额管理)
│   ├── AiCacheService (相似问题缓存,避免重复调用)
│   └── 流式响应支持 (SSE: text/event-stream)
│
├── 配置化:
│   └── application.yml:
│       ai:
│         provider: deepseek
│         model: deepseek-chat
│         temperature: 0.7
│         max-tokens: 4096
│         enable-stream: true
│         daily-limit-per-user: 100
│         cache-similarity-threshold: 0.85
│
└── 前端体验:
    ├── 流式打字效果 (逐字显示AI回复)
    ├── AI 回复支持 Markdown 渲染
    ├── 快捷操作栏 (复制/重新生成/点赞/点踩)
    └── 用量显示 (今日剩余 N 次)
```

---

## 三、时间与进度管理（Phase 1）

### 3.1 日历模块

```
数据库:
CREATE TABLE calendar_events (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    project_id BIGINT DEFAULT NULL,
    title VARCHAR(200) NOT NULL,
    description TEXT,
    start_time DATETIME NOT NULL,
    end_time DATETIME NOT NULL,
    all_day TINYINT(1) NOT NULL DEFAULT 0,
    event_type ENUM('TASK_DEADLINE','MEETING','REMINDER','SPRINT','CUSTOM') NOT NULL 'TASK_DEADLINE',
    related_id BIGINT DEFAULT NULL,           -- 关联issue_id/sprint_id
    related_type VARCHAR(20) DEFAULT NULL,    -- ISSUE / SPRINT
    color VARCHAR(20) DEFAULT '#409EFF',
    location VARCHAR(200),
    participants JSON,                        -- 参与人ID列表
    reminder_minutes INT DEFAULT 15,          -- 提前提醒(分钟)
    created_by BIGINT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_time (user_id, start_time),
    INDEX idx_project (project_id),
    INDEX idx_related (related_type, related_id)
);

后端:
├── entity/dto/mapper/service/controller: CalendarEvent*
└── service: CalendarSyncService (任务截止日期→自动同步日历事件)

前端:
├── views/CalendarView.vue (FullCalendar或自绘月/周/日视图)
├── components/
│   ├── EventCard.vue
│   ├── EventForm.vue (创建/编辑弹窗)
│   └── CalendarGrid.vue
└── api: calendar.ts

功能点:
├── 月视图 / 周视图 / 日视图 / 日程列表 切换
├── 任务 dueDate 自动同步到日历(只读标记)
├── 拖拽创建事件 / 拖拽调整时间
├── 事件颜色按类型区分 (截止日期=红/会议=蓝/Sprint=绿)
├── 提醒通知集成 (到时间→WebSocket推送)
└── 与看板联动: 点击日历上的任务→跳转 IssueDetailDialog
```

### 3.2 甘特图视图

```
后端:
├── service/GanttService.java:
│   ├── getGanttData(projectId) → 返回任务时间线+依赖关系
│   └── updateGanttTimeline(issueId, startDate, endDate) // 拖拽更新
└── controller/GanttController.java

前端:
├── views/GanttView.vue (作为 projects/:id/gantt 路由)
├── components/
│   ├── GanttRow.vue (行: 任务名称+负责人+起止时间条)
│   ├── GanttBar.vue (可拖拽的任务条)
│   ├── GanttTimeline.vue (时间轴头)
│   ├── GanttDependency.vue (任务间依赖连线)
│   └── GanttToolbar.vue (缩放/筛选/今日定位)
├── api: gantt.ts
└── 推荐库: @antv/g6 或 gantt-task-react (轻量)

数据库扩展:
├── issues 表增加字段 (如无):
│   ├── start_date DATE (计划开始日期, 区别于created_at)
│   └── predecessor_ids JSON (前置任务ID列表, 用于依赖连线)
└── 或新建表 task_dependencies (id, from_issue_id, to_issue_id, dependency_type)

功能点:
├── 左侧任务树 (支持父子层级折叠)
├── 右侧时间轴 (日/周/月缩放)
├── 任务条拖拽 (调整起止日期)
├── 依赖关系可视化 (箭头连线)
├── 里程碑标记 (菱形图标)
├── 进度条内嵌 (已完成%灰色填充)
├── 今日线 (红色竖线)
├── 关键路径高亮 (橙色)
├── 与看板双向同步 (甘特图拖拽→更新task.dueDate)
└── 筛选 (按负责人/优先级/类型)
```

### 3.3 工时记录系统

```
数据库:
CREATE TABLE time_logs (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    issue_id BIGINT NOT NULL,
    project_id BIGINT NOT NULL,
    hours DECIMAL(5,2) NOT NULL COMMENT '工时数',
    description TEXT COMMENT '工作内容说明',
    log_date DATE NOT NULL COMMENT '日期',
    start_time DATETIME DEFAULT NULL,       -- 计时器开始时间
    end_time DATETIME DEFAULT NULL,         -- 计时器结束时间
    is_running TINYINT(1) NOT NULL DEFAULT 0, -- 是否正在计时
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user_date (user_id, log_date),
    INDEX idx_issue (issue_id),
    INDEX idx_project_date (project_id, log_date)
);

后端:
├── entity/dto/mapper/controller: TimeLog*
├── service:
│   ├── TimeLogService (CRUD + 计时器启停)
│   └── WorkloadStatisticsService (聚合统计)
│       ├── getUserWorkload(userId, startDate, endDate)
│       ├── getProjectWorkload(projectId, sprintId)
│       └── exportTimeReport(projectId, format) → Excel/CSV

前端:
├── views/TimeLogView.vue (工时列表+统计)
├── components/
│   ├── TimeTracker.vue (浮动计时器组件: 开始/暂停/停止)
│   ├── TimeLogForm.vue (手动录入表单)
│   ├── TimeReportChart.vue (ECharts工时报表: 柱状图/热力图)
│   └── WeeklyTimesheet.vue (周工时表: 类Excel网格编辑)
├── api: time-log.ts
└── 看板集成: IssueDetailDialog 新增 "工时记录" Tab

功能点:
├── 在线计时器 (一键开始→最小化到角落→停止→自动填入)
├── 手动录入 (日期+工时数+说明)
├── 按任务/人员/日期/Sprint 筛选
├── 个人工时报表 (日/周/月维度)
├── 项目工时分布 (成员对比柱状图)
├── 与Sprint关联 (Sprint工时预算 vs 实际)
├── 导出 Excel (财务/PMO需求)
└── 工时预警 (单日超10h / 连续7天无休息)
```

---

## 四、流程与权限增强（Phase 2）

### 4.1 可配置工作流引擎

> 替换当前硬编码的 TODO → IN_PROGRESS → DONE 三状态

```
数据库:
CREATE TABLE workflows (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    project_id BIGINT NOT NULL,
    name VARCHAR(100) NOT NULL COMMENT '工作流名称',
    issue_type VARCHAR(20) NOT NULL COMMENT 'STORY/TASK/BUG',
    config JSON NOT NULL COMMENT '节点+转换规则定义',
    is_active TINYINT(1) NOT NULL DEFAULT 1,
    is_default TINYINT(1) NOT NULL DEFAULT 0,
    created_by BIGINT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_project_type (project_id, issue_type),
    INDEX idx_project (project_id)
);

-- config JSON 结构示例:
-- {
--   "nodes": [
--     { "id": "backlog", "name": "待办", "color": "#909399", "type": "column" },
--     { "id": "ready", "name": "就绪", "color": "#409EFF", "type": "column" },
--     { "id": "progress", "name": "开发中", "color": "#E6A23C", "type": "column" },
--     { "id": "review", "name": "待验证", "color": "#67C23A", "type": "column" },
--     { "id": "done", "name": "已完成", "color": "#F56C6C", "type": "column", "isTerminal": true },
--     { "id": "closed", "name": "已关闭", "color": "#909399", "type": "column", "isTerminal": true }
--   ],
--   "transitions": [
--     { "from": "backlog", "to": "ready", "conditions": ["ROLE_ADMIN","ASSIGNEE"] },
--     { "from": "ready", "to": "progress", "conditions": ["ASSIGNEE"] },
--     { "from": "progress", "to": "review", "conditions": ["ASSIGNEE"] },
--     { "from": "review", "to": "done", "conditions": ["REPORTER"] },
--     { "from": "review", "to": "progress", "conditions": ["ASSIGNEE"] },  // 打回
--     { "from": "done", "to": "closed", "conditions": ["ROLE_ADMIN"] }
--   ]
-- }

后端:
├── entity: Workflow.java, WorkflowNode.java, WorkflowTransition.java
├── service:
│   ├── WorkflowDesignerService (CRUD + 校验)
│   └── WorkflowEngineService (核心状态机):
│       ├── validateTransition(workflowId, fromStatus, toStatus, userId)
│       ├── getAvailableTransitions(workflowId, currentStatus, userId)
│       └── executeTransition(issueId, toStatus, userId)
├── 改造 BoardService.moveIssue() → 先校验工作流合法性
└── controller: WorkflowController.java

前端:
├── views/WorkflowDesigner.vue (可视化设计器)
│   ├── 节点拖拽添加/删除/重命名
│   ├── 连线绘制转换规则
│   ├── 条件配置弹窗 (谁能执行此转换)
│   └── 预设模板选择 (Scrum/Kanban/Bug跟踪/ITSM)
├── components/workflow/
│   ├── WorkflowCanvas.vue (画布)
│   ├── WorkflowNode.vue (可拖拽节点)
│   ├── TransitionLine.vue (贝塞尔曲线连线)
│   └── ConditionEditor.vue (条件配置)
└── api: workflow.ts

预设模板:
├── Scrum (5状态): Backlog → Ready → In Progress → Review → Done
├── Kanban (3状态): Todo → Doing → Done (当前默认)
├── Bug Tracking (7状态): New → Confirmed → In Progress → Resolved → Verified → Closed → Reopened
└── IT Service: Open → In Progress → Pending → Resolved → Closed
```

### 4.2 细粒度 RBAC 权限系统

```
数据库:
CREATE TABLE permissions (
    id INT AUTO_INCREMENT PRIMARY KEY,
    code VARCHAR(50) NOT NULL UNIQUE COMMENT '权限编码',
    name VARCHAR(50) NOT NULL COMMENT '权限名称',
    module VARCHAR(30) NOT NULL COMMENT '所属模块',
    description VARCHAR(200),
    sort_order INT DEFAULT 0
);

CREATE TABLE roles (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL COMMENT '角色名',
    code VARCHAR(50) NOT NULL UNIQUE COMMENT '角色编码',
    is_system TINYINT(1) NOT NULL DEFAULT 0 COMMENT '系统内置不可删',
    description VARCHAR(200),
    sort_order INT DEFAULT 0
);

CREATE TABLE role_permissions (
    role_id INT NOT NULL,
    permission_id INT NOT NULL,
    PRIMARY KEY (role_id, permission_id)
);

-- 扩展 team_members 表:
-- ALTER TABLE team_members ADD COLUMN custom_role_id INT DEFAULT NULL;
-- 或用 JSON: role_overrides JSON (针对某项目/团队的权限覆盖)

预置权限点清单:
├── 项目模块:
│   ├── project.create / project.view / project.edit / project.delete
│   ├── project.member.invite / project.member.remove / project.member.manage
│   └── project.settings
├── 任务模块:
│   ├── issue.create / issue.view / issue.edit / issue.delete
│   ├── issue.assign / issue.move / issue.change-status
│   ├── issue.comment / issue.attach
│   └── issue.manage-labels
├── Sprint模块:
│   ├── sprint.create / sprint.start / sprint.complete
│   └── sprint.manage (增删改查全部)
├── 看板模块:
│   ├── board.view / board.configure (增删列/泳道)
│   └── board.export
├── 报告模块:
│   ├── report.create / report.view / report.export
│   └── report.manage-all (查看所有人报告)
├── 团队模块:
│   ├── team.create / team.manage / team.member.manage
│   └── team.settings
├── 系统管理:
│   ├── user.manage / level.manage / approval.manage
│   ├── audit.log.view / settings.manage
│   └── ai.manage (AI用量管理)
└── Wiki模块 (Phase 2.3):
    ├── wiki.create / wiki.edit / wiki.delete
    └── wiki.admin

预置角色:
├── 项目经理 (PROJECT_MANAGER): 项目全权限 + 团队成员管理
├── 开发者 (DEVELOPER): 创建/编辑/移动任务 + 评论 + 工时
├── 测试人员 (TESTER): 创建Bug + 验证关闭 + 评论
├── 产品负责人 (PRODUCT_OWNER): Story管理 + 优先级 + Sprint规划
├── 访客 (VIEWER): 只读查看
└── 报表管理员 (REPORTER): Dashboard + 报表导出

后端实现:
├── @RequirePermission("issue.delete") 自定义注解
├── PermissionService.hasPermission(userId, permissionCode)
├── AOP切面: PermissionAspect (方法级拦截)
└── 改造 SecurityConfig / JwtAuthenticationFilter

前端实现:
├── v-permission 指令: <el-button v-permission="'issue.delete'">
├── composable: usePermission()
├── views/RoleManage.vue (角色CRUD + 权限矩阵勾选)
└── 路由守卫集成 (菜单级隐藏)
```

### 4.3 Wiki 文档系统

```
数据库:
CREATE TABLE wiki_pages (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    project_id BIGINT NOT NULL,
    parent_id BIGINT DEFAULT NULL COMMENT '父页面ID(树形结构)',
    title VARCHAR(200) NOT NULL,
    content LONGTEXT COMMENT 'Markdown内容',
    author_id BIGINT NOT NULL,
    version INT NOT NULL DEFAULT 1,
    sort_order INT DEFAULT 0,
    view_count INT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_project_parent (project_id, parent_id),
    INDEX idx_project (project_id),
    FULLTEXT INDEX ft_content (title, content)
);

CREATE TABLE wiki_versions (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    page_id BIGINT NOT NULL,
    version INT NOT NULL,
    content LONGTEXT NOT NULL,
    editor_id BIGINT NOT NULL,
    change_summary VARCHAR(500) COMMENT '变更摘要',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_page (page_id, version)
);

CREATE TABLE wiki_attachments (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    page_id BIGINT NOT NULL,
    file_name VARCHAR(255) NOT NULL,
    file_path VARCHAR(500) NOT NULL,
    file_size BIGINT NOT NULL,
    uploader_id BIGINT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_page (page_id)
);

后端: 完整 MVC (WikiPage* + WikiVersion*)
前端:
├── views/WikiView.vue (左右布局: 左侧目录树 + 右侧编辑/预览)
├── components/wiki/
│   ├── WikiTree.vue (可拖拽排序的目录树)
│   ├── WikiEditor.vue (SplitPane: 左Markdown右预览)
│   ├── WikiPreview.vue (渲染后展示)
│   ├── WikiVersionHistory.vue (版本列表+Diff对比)
│   └── WikiSearch.vue (全文检索结果)
└── api: wiki.ts

功能点:
├── Markdown编辑 + 实时预览 (marked.js + highlight.js代码高亮)
├── 树形目录 (无限层级/拖拽排序/折叠展开)
├── 版本历史 (查看/回滚/Diff对比)
├── 文件上传 (图片粘贴上传/拖拽上传)
├── 全文检索 (标题+内容)
├── @提及用户 (同聊天功能)
├── 导出 PDF / Word
└── 与任务联动: [[页面标题]] 语法自动链接
```

---

## 五、效率提升与生态（Phase 3）

### 5.1 自动化规则引擎

```
数据库:
CREATE TABLE automation_rules (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    project_id BIGINT NOT NULL,
    name VARCHAR(100) NOT NULL,
    enabled TINYINT(1) NOT NULL DEFAULT 1,
    trigger_type VARCHAR(30) NOT NULL COMMENT '触发器类型',
    trigger_config JSON NOT NULL COMMENT '触发条件配置',
    action_type VARCHAR(30) NOT NULL COMMENT '动作类型',
    action_config JSON NOT NULL COMMENT '动作参数配置',
    run_count INT DEFAULT 0 COMMENT '执行次数',
    last_run_at DATETIME DEFAULT NULL,
    created_by BIGINT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_project (project_id)
);

CREATE TABLE automation_logs (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    rule_id BIGINT NOT NULL,
    trigger_event JSON COMMENT '触发时的上下文',
    action_result JSON COMMENT '执行结果',
    status VARCHAR(20) NOT NULL COMMENT 'SUCCESS/FAILED/SKIPPED',
    error_message TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_rule (rule_id)
);

支持的触发器 (trigger_type):
├── ISSUE_CREATED        // 任务创建
├── ISSUE_UPDATED        // 任务更新(可指定字段)
├── STATUS_CHANGED       // 状态变更(可指定 from→to)
├── COMMENT_ADDED        // 新增评论
├── DUE_APPROACHING      // 截止日期临近(N小时内)
├── DUE_PASSED           // 已逾期
├── SPRINT_STARTED       // Sprint开始
├── SPRINT_COMPLETED     // Sprint结束
└── SCHEDULED            // 定时触发(cron表达式)

支持的动作 (action_type):
├── ASSIGN_TO            // 指派人 (固定用户/轮询/负载最低)
├── CHANGE_STATUS        // 修改状态
├── CHANGE_PRIORITY      // 修改优先级
├── ADD_LABEL            // 添加标签
├── SEND_NOTIFICATION    // 发送通知 (站内/邮件/钉钉/飞书)
├── CREATE_COMMENT       // 自动评论
├── CREATE_SUB_TASK      // 创建子任务
├── CALL_AI              // 调用AI (分析/建议)
├── HTTP_WEBHOOK         // 发送HTTP请求
└── SET_FIELD            // 设置任意字段值

预设规则模板:
├── "新Bug自动分配给默认测试负责人"
├── "任务逾期→发送钉钉告警"
├── "任务完成→通知创建者"
├── "High优先级任务→发送紧急通知"
├── "Sprint结束时自动生成回顾草稿"
└── "每日下午5点→发送明日待办摘要"
```

### 5.2 第三方集成

```
数据库:
CREATE TABLE integrations (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    project_id BIGINT NOT NULL,
    type VARCHAR(30) NOT NULL COMMENT 'DINGTALK/FEISHU/EMAIL/WEBHOOK/GITLAB/GITHUB',
    name VARCHAR(100) NOT NULL,
    config JSON NOT NULL COMMENT '加密存储的凭证配置',
    enabled TINYINT(1) NOT NULL DEFAULT 1,
    last_sync_at DATETIME DEFAULT NULL,
    created_by BIGINT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_project_type (project_id, type)
);

集成能力:
├── 钉钉 (DingTalk):
│   ├── Robot Webhook (群消息推送)
│   ├── WorkFlow审批回调
│   └── 用户同步 (可选)
├── 飞书 (Feishu/Lark):
│   ├── Webhook机器人
│   ├── 飞书文档嵌入
│   └── 日历同步 (双向)
├── 邮件 (SMTP):
│   ├── 日报/周报邮件发送
│   ├── @提及邮件通知
│   └── 审批结果邮件
├── Git平台:
│   ├── GitLab/Gitea Webhook → 自动创建Bug/Task
│   ├── Commit消息关联任务 (fixes #123)
│   └── MR/PR状态同步
└── 通用Webhook:
    └── 事件订阅 → HTTP回调 (开放API)
```

### 5.3 数据导入导出增强

```
后端 ExportService:
├── GET /api/export/project/{id}/excel    → 任务Excel (.xlsx)
├── GET /api/export/project/{id}/csv      → CSV
├── GET /api/export/project/{id}/json     → 完整JSON备份
├── GET /api/export/project/{id}/pdf      → 项目报告PDF
├── POST /api/import/project/{id}/excel   → Excel批量导入任务
└── POST /api/import/jira                → Jira XML迁移

Excel导出字段:
├── 任务Key / 类型 / 标题 / 状态 / 优先级
├── 指派人 / 报告人 / Sprint
├── 故事点 / 截止日期 / 标签
├── 描述 (前200字)
└── 工时总计 (如有time_logs)

前端: ProjectOverview.vue 增加 导出/导入 按钮组
```

### 5.4 PWA 移动端适配

```
新增内容:
├── public/
│   ├── manifest.json (应用清单)
│   ├── icons-192.png / icons-512.png / maskable-icon.png
│   └── sw.js (Service Worker: 离线缓存静态资源)
├── src/composables/useOffline.ts (离线检测+队列)
├── src/styles/responsive.css (断点优化)
├── vite.config.ts (vite-plugin-pwa)
└── 移动端布局改造:
    ├── 看板: 单列水平滑动 (touch-scroll)
    ├── 底部Tab导航 (替代侧边栏)
    ├── 任务卡片: 触控友好 (更大点击区/长按菜单)
    ├── 表单: 全屏弹窗 (头部固定+底部按钮)
    ├── 聊天: 类微信界面
    └── Dashboard: 卡片网格自适应
```

---

## 六、体验优化与商业化准备（Phase 4）

### 6.1 高级报表与分析

```
新增报表视图: views/AdvancedReports.vue

报表类型:
├── 速度图 (Velocity Chart):
│   └── 每个Sprint的承诺故事点 vs 完成故事点 趋势
├── 累积流图 (CFD - Cumulative Flow Diagram):
│   └── 各状态任务数量随时间的堆叠面积图
├── 分布图 (Cycle Time Distribution):
│   └── 任务从创建到完成的耗时分布直方图
├── 资源利用率热力图:
│   └── 成员×日期 的工时投入热力 (类GitHub贡献图)
├── Bug趋势图:
│   └── 新建/解决/累积 Bug 数量折线图
├── 自定义报表构建器:
│   └── 拖拽选指标(X/Y轴) + 图表类型 + 筛选条件
└── 所有报表支持:
    ├── 日期范围选择
    ├── 按 项目/Sprint/成员 筛选
    ├── 导出 PNG/PDF/Excel
    └── 定时邮件发送 (结合自动化引擎)
```

### 6.2 项目模板市场

```
预设模板 (每种包含: 工作流定义 + 字段配置 + 默认列 + 权限建议):

├── 敏捷开发 (Scrum Enhanced):
│   ├── 5状态工作流 + Story Map视图
│   ├── Story/Task/Bug + Spike(技术调研) 类型
│   └── Planning Checklist 模板
├── 缺陷跟踪 (Bug Tracking Pro):
│   ├── 7状态Bug生命周期
│   ├── 严重程度/环境/复现步骤 字段组
│   └── Bug分类统计仪表盘
├── IT服务管理 (ITSM):
│   ├── Incident / Problem / Change 三种工单
│   ├── SLA响应时效追踪
│   └── CMDB关联(预留)
├── 产品研发 (Product Dev):
│   ├── Idea → PRD → Design → Dev → Test → Release
│   ├── 需求池优先级矩阵
│   └── 版本发布规划
├── 市场营销 (Marketing):
│   ├── Campaign策划→执行→复盘 全流程
│   ├── ROI追踪字段
│   └── 素材管理(附件增强)
├── HR招聘 (Recruitment):
│   ├── 简历筛选→面试→Offer→入职
│   ├── 候选人评分卡
│   └── 面试官日历
└── 客户成功 (Customer Success):
    ├── 客户Onboarding检查清单
    ├── 工单处理SLA
    └── QBR(季度业务回顾)模板
```

### 6.3 国际化 (i18n)

```
技术方案: vue-i18n + 后端 MessageSource

支持语言:
├── 简体中文 (zh-CN) - 默认
├── English (en-US)
└── 日本語 (ja-JP) - 可选

实现:
├── frontend/src/locales/*.json (翻译文件)
├── frontend/src/i18n.ts (配置)
├── frontend/src/composables/useI18n.ts
├── 后端异常消息国际化 (Accept-Language header)
└── 语言切换: 用户设置 → localStorage持久化
```

### 6.4 操作审计增强

```
数据库 activity_logs 扩展:
ALTER TABLE activity_logs
ADD COLUMN ip_address VARCHAR(45) COMMENT '操作IP',
ADD COLUMN user_agent VARCHAR(500) COMMENT '浏览器UA',
ADD COLUMN diff_json JSON COMMENT '字段变更Diff',
ADD COLUMN request_id VARCHAR(36) COMMENT '请求追踪ID';

新增:
├── AOP审计切面: @AuditLog (自动记录所有写操作)
├── views/AuditLogView.vue (管理员审计日志查看器)
│   ├── 时间线展示
│   ├── 变更Diff高亮 (before→after)
│   ├── 按用户/操作类型/时间 筛选
│   └── 导出审计日志
└── 异常行为检测:
    ├── 短时间大量删除 (>10次/分钟)
    ├── 非工作时间敏感操作
    └── 异地登录IP变更告警
```

---

## 七、技术债务清理（穿插执行）

| 问题 | 影响范围 | 修复时机 | 方案 |
|------|---------|---------|------|
| `@PreAuthorize` role前缀bug | TeamController权限失效 | **Phase 0前必修** | `hasRole('ADMIN')` → `hasAuthority('ROLE_ADMIN')` |
| Redis未使用 | 无法水平扩展Session | Phase 1 | 接入Redis缓存+Spring Session |
| 上传目录未配置(mimo.upload.dir) | 附件功能可能异常 | **立即修复** | application.yml添加配置 |
| WebSocket内存broker | 多实例不同步 | Phase 3 | 切换Redis Pub/Sub (STOMP Relay) |
| 单元测试覆盖率低 | 回归风险 | 每个Phase交付时补充 | 核心Service≥70%覆盖率 |
| JWT密钥硬编码 | 安全隐患 | Phase 0 | 改为环境变量 + 定期轮换 |
| AI API Key明文配置 | 安全隐患 | Phase 0 | 加密存储或Vault |
| 前端无ESLint/Prettier统一 | 代码风格不一致 | Phase 0前 | 配置并加入pre-commit hook |

---

## 八、实施路线图总览

```
时间线 (建议):

Week 1-2:  ══════════════════════════════
           ★ Phase 0: 技术债务修复 + AI基础设施
           ├─ 修复PreAuthorize/上传目录/JWT/ApiKey安全问题
           ├─ AI Provider抽象层 + 流式响应
           ├─ AI智能任务创建 (parse-issue)
           └─ AI日报/周报增强

Week 3-4:  ══════════════════════════════
           ★ Phase 0续 + Phase 1启动
           ├─ AI Sprint规划助手
           ├─ AI智能问答(RAG基础版)
           ├─ 日历模块 (月/周视图 + 任务同步)
           └─ 补充单元测试 (AiController + CalendarService)

Week 5-6:  ══════════════════════════════
           ★ Phase 1 核心
           ├─ 甘特图视图 (依赖@antv/g6)
           ├─ 工时记录系统 (计时器+报表)
           └─ Sprint燃尽图可视化 (补上前端缺失!)

Week 7-8:  ══════════════════════════════
           ★ Phase 2 启动
           ├─ 可配置工作流引擎 (核心状态机)
           ├─ 工作流可视化设计器
           └─ RBAC权限系统 (注解+AOP+前端指令)

Week 9-10: ══════════════════════════════
           ★ Phase 2 续
           ├─ Wiki文档系统 (Markdown+版本历史)
           ├─ 权限系统全模块覆盖
           └─ 工作流预设模板 (Scrum/Kanban/Bug/ITSM)

Week 11-12:══════════════════════════════
           ★ Phase 3
           ├─ 自动化规则引擎 (触发器+动作)
           ├─ 第三方集成 (钉钉/飞书/邮件)
           └─ 数据导入导出 (Excel/CSV/Jira)

Week 13+:  ══════════════════════════════
           ★ Phase 3-4
           ├─ PWA移动端适配
           ├─ 高级报表 (速度图/CFD/自定义)
           ├─ 项目模板市场
           └─ 国际化(i18n)
```

---

## 九、各阶段交付物清单

### Phase 0 交付物 (AI 增强)
- [x] AI Provider 抽象层 (多模型支持) — v2.7.3
- [x] AI 流式响应 (SSE) — v2.8.0 基础，**v2.9.0 重构为 AsyncContext + POST，彻底解决 406/ERR_ABORTED**
- [x] AI 智能任务创建 (`/api/ai/parse-issue`) — v2.8.0
- [x] AI Story 拆分 (`/api/ai/suggest-tasks`) — v2.8.0
- [x] AI 故事点估算 (`/api/ai/estimate-story-points`) — v2.8.0
- [ ] AI Sprint 规划助手 (3个接口) — 后续
- [ ] AI 日报/周报生成增强 (2个接口) — 后续
- [x] AI 智能问答面板 (前端组件) — v2.8.0
- [x] AI 用量统计与配额管理 (`/api/ai/usage`) — v2.8.0
- [ ] 技术债务修复 (4项必修) — 部分完成

### Phase 1 交付物 (时间管理)
- [x] 日历模块 (全功能) — v2.8.0 基础 + **v2.9.0 团队可见性/项目关联/筛选**
- [ ] 甘特图视图 (依赖关系+拖拽) — 待启动
- [ ] 工时记录系统 (计时器+报表) — 待启动
- [x] **Sprint 燃尽图前端可视化** (补缺!) — v2.9.1 (BurndownChart 组件 + SprintView 管理页 + ProjectOverview 当前活跃 Sprint 卡片)
- [ ] 相关单元测试 — 进行中

### Phase 2 交付物 (流程权限)
- [ ] 工作流引擎 (状态机核心)
- [ ] 工作流可视化设计器
- [ ] 4种预设工作流模板
- [ ] RBAC 权限系统 (6角色 × 30+权限点)
- [ ] Wiki 文档系统
- [ ] 权限管理后台

### Phase 3 交付物 (效率生态)
- [ ] 自动化规则引擎 (8触发器 × 10动作)
- [ ] 钉钉/飞书/邮件集成
- [ ] Excel/CSV 导入导出
- [ ] PWA 移动端适配

### Phase 4 交付物 (体验商业化)
- [ ] 高级报表 (6种图表)
- [ ] 自定义报表构建器
- [ ] 7种项目模板
- [ ] 国际化 (中/英/日)
- [ ] 操作审计增强

---

## 十、风险评估与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| AI API 成本超支 | 中 | 高 | 设置用户配额 + 相似问题缓存 + 本地模型备选 |
| 工作流引擎复杂度爆炸 | 高 | 高 | 先做有限状态机(不支持循环)+预设模板为主 |
| 甘特图性能问题(大量任务) | 中 | 中 | 虚拟滚动 + 分页加载 + 按Sprint筛选 |
| 权限系统迁移兼容性 | 中 | 高 | 旧角色映射 + 灰度切换开关 |
| 第三方API变动 | 低 | 中 | 抽象Adapter模式 + Mock测试 |
| PWA离线数据一致性 | 高 | 中 | 操作队列 + 冲突解决策略(最后写入获胜) |

---

## 十一、版本变更记录

### v2.9.0 (2026-06-17) — 日历增强 + AI 流式稳定化

**新增/改进**:
- 日历模块：team_id 字段 (V7 迁移) + 团队可见性规则 + 项目关联 + 团队/类型筛选
- 日历：EventForm 增加 team→project 级联选择，ProjectOverview 集成"创建事件"动作
- AI 助手 SSE：后端改用 `javax.servlet.AsyncContext` 原生异步 API + `void` 返回 + 主线程 `flushBuffer()`，彻底消除 `HttpMediaTypeNotAcceptableException (406)` 和 `HttpMessageNotWritableException`
- AI 助手 SSE：前端 `ai.ts` 从 `GET` + URL 参数改为 `POST` + JSON body，避开浏览器 URL 长度限制
- AI 助手 SSE：`AiChatPanel` 在发送新消息前 `abortController.abort()` 终止上一条流，消除控制台 `net::ERR_ABORTED` 噪音
- AI 助手 SSE：`DeepSeekProvider.chatStream` 改用 `BodyHandlers.ofInputStream()` + 逐行解析 SSE chunks
- `GlobalExceptionHandler` 不再拦截 `/api/ai/chat/stream`，让异步派发自然完成
- Vite 代理针对 SSE 关闭 `changeOrigin` 缓冲

**Bug 修复**: BUG-049 / BUG-050 / BUG-051 / BUG-052 (详见 `bug-log.md` v2.9.0)

**未解决（已知问题）**:
- AI 助手 systemPrompt 硬编码，无项目/任务上下文自动注入
- AI 流式响应不支持断线重连

### v2.8.0 (2026-06-16) — AI Copilot + 粒子动画 + 构建修复

**新增/改进**:
- AI Copilot 集成：DeepSeek 7 个接口 + 3 个前端组件
- 8 个 TypeScript 构建错误修复 + `init.html` 粒子动画
- `ProjectBoard` 编码 / SCSS / 语法修复
- 4 大文档同步到 v2.8.0

### v2.7.3 (2026-06-15) — Apple-grade UI 升级

- 13 个文件 Apple 风格精修
- Dashboard、Profile、AuthLayout、MainLayout 等关键页面升级

---

*文档版本: v2 | 最后更新: 2026-06-17 | 维护者: Mimo Team*
